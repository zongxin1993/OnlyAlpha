# Market Conformance Query

`OnlyMarketProfileQueryService` 是首个稳定只读边界，返回 JSON-ready summary/detail DTO。它读取 Registry public API，不执行规则、
不运行 Scenario、不访问 Runtime Manager。Conformance run repository 和相应查询 DTO 尚未完成。
