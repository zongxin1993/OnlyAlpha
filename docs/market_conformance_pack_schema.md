# Market Conformance Pack Schema

Pack identity 由 `pack_id + version` 组成，并声明 Profile family/version、稳定排序的 Scenario bindings、capability requirements、
schema version 和 source。Requirement 包含 capability、required Scenario IDs 与 minimum pass count。Pack 不定义 Settlement、Fee、
Margin、Matching 或其他规则算法。
