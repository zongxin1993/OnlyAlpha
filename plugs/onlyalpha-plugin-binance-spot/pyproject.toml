[project]
name = "onlyalpha-plugin-binance-spot"
version = "0.9.8"
description = "Binance Spot Market Product plugin for OnlyAlpha"
readme = "README.md"
requires-python = ">=3.12,<3.13"
dependencies = ["onlyalpha==0.9.8"]

[project.entry-points."onlyalpha.market_products"]
binance-spot = "onlyalpha_plugin_binance_spot.factory:OnlyBinanceSpotMarketProductFactory"

[project.entry-points."onlyalpha.backtest_market_product_resources"]
binance-spot-reference = "onlyalpha_plugin_binance_spot.resource_provider:OnlyBinanceSpotReferenceResourceProvider"

[build-system]
requires = ["hatchling>=1.26,<2"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/onlyalpha_plugin_binance_spot"]

[tool.uv.sources]
onlyalpha = { workspace = true }

[tool.mypy]
python_version = "3.12"
strict = true
files = ["src/onlyalpha_plugin_binance_spot"]
