# Account Demo

使用 `OnlyAccountManager` 的正式 Command/Query 接口初始化现金账户、建立/释放 Reservation，并读取 immutable Snapshot。

```bash
UV_CACHE_DIR=/tmp/onlyalpha-uv-cache uv run python -m examples.account_demo.run_demo
```
