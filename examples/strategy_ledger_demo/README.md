# Strategy Ledger demos

These scripts demonstrate fixed capital, buy/sell accounting, Cluster isolation, cash Reservation,
Allocation valuation, drawdown, deterministic replay, and the read-only Context view. Run them from
this directory so `_support.py` is importable.
