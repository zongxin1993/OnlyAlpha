你正在实现 OnlyAlpha P7.8。

Repository:
https://github.com/zongxin1993/OnlyAlpha

任务名称：

P7.8 — Research Result Authority & Deterministic Research Output Closure

Engineering Companion Closure:

Task Gate Persistence & Scoped Verification Closure


======================================================================
0. 最高工程原则
======================================================================

不要把本 Prompt 当成 Repository 当前事实来源。

当前 Repository 的：

- default branch
- exact HEAD SHA
- 当前源码
- 当前正式测试
- 未被替代的 ADR
- AGENTS.md
- docs/engineering/**
- scripts/test_suite.py
- scripts/verify.py
- GitHub workflow

才是 current truth。

如果本 Prompt 与当前 Repository 冲突：

Repository wins.

但不要因为 Repository 当前实现存在缺陷，
就机械维持缺陷。

发现冲突时必须从第一性原理判断：

1. Repository 当前事实是什么；
2. 本 Prompt 原设计解决的根本问题是什么；
3. 当前实现是否已经以更好的方式解决；
4. 如果没有，应该修复设计还是修复实现；
5. 最终采用什么方案以及为什么。

在最终报告中记录重要偏差。


======================================================================
1. 永远从第一性原理解决问题
======================================================================

OnlyAlpha 不接受：

“为了赶进度先实现”
“为了测试通过加 workaround”
“为了保险把所有测试都跑一遍”
“因为不知道影响范围所以直接 full repository”
“为了方便新建第二套 authority”
“为了以后可能有用先建立通用框架”
“为了 CI 变绿改变正确业务语义”

任何重要设计必须先回答：

1. 真正的问题是什么？
2. 谁拥有这份状态？
3. 谁是唯一 authoritative source？
4. 谁可以修改？
5. 谁只能读取？
6. 哪些字段属于 semantic identity？
7. 哪些只是 invocation / provenance / presentation？
8. 已有哪个正式 authority 可以复用？
9. 是否正在复制已有 authority？
10. 失败发生在哪里？
11. corruption 如何处理？
12. restart / retry / reuse 后是否收敛到相同结果？
13. fresh process 是否得到相同 identity？
14. physical location 是否错误进入 semantic identity？
15. 最小设计是什么？
16. 如何用自动化约束防止未来架构回退？

工程决策优先级：

正确性
>
架构一致性
>
可验证性
>
可恢复 / 可重入性
>
可维护性
>
性能
>
自动化程度


======================================================================
2. 本任务不是“每个步骤重新审计 Repository”
======================================================================

P7.8 开始时执行一次 Current Truth Audit。

完成 Audit 后冻结：

TASK_BASE_SHA=<P7.8 开始时 exact HEAD SHA>

后续 Block A / B / C / D 默认复用这个 Task Context。

不要每完成一个 Block 就重新：

- 扫描整个 Repository；
- 阅读全部 workflow；
- 阅读全部 reports；
- 分析全部测试；
- 重新决定 pytest 范围。

只有出现以下情况才重新审查相应范围：

- external HEAD change；
- merge / rebase；
- actual modification scope 超出预期；
- public contract 发生未预期变化；
- Current Truth 与 Task Contract 出现实质冲突。


======================================================================
3. Current Truth Audit
======================================================================

第一阶段不要修改代码。

一次性确认：

1. default branch；
2. exact HEAD SHA；
3. 当前 version；
4. P7 milestone 当前状态；
5. latest VERIFIED increment；
6. P7.7 当前真实状态；
7. P7 Phase Gate 是否未完成；
8. P7 Final Certification 是否未完成；
9. Research Runtime 是否仍 unsupported；
10. Live Runtime 是否仍 unsupported；
11. 当前 Research 已实现：
    - Dataset
    - Calculation
    - Calculation Result
    - Job
    - Factor
    - Sweep
    - Target
    - Statistics
    - Statistics Result
12. 当前是否仍不存在正式 Research Result authority；
13. 当前 Statistics Result identity/store contract；
14. 当前 Sweep Outcome 是否仍是 invocation evidence 而不是 durable research authority；
15. scripts/test_suite.py 当前 canonical lanes；
16. scripts/verify.py 当前 Impact Resolver；
17. quality-system.md 当前三层验收定义；
18. quality-toolchain.md 当前工具策略；
19. AGENTS.md 是否已完整绑定新的三层验收；
20. 当前 workflows 是否与质量语义冲突；
21. roadmap / architecture 是否存在事实漂移。

重点读取：

AGENTS.md
README.md
docs/roadmap.md
docs/architecture.md
docs/architecture_principles.md
docs/engineering/quality-system.md
docs/engineering/quality-toolchain.md
docs/adr/**
相关 docs/reports/**

scripts/test_suite.py
scripts/verify.py
scripts/certification.py

.github/workflows/quality.yml
.github/workflows/certification.yml

src/onlyalpha/research/**
src/onlyalpha/calculation/**

tests/research/**
tests/architecture/**

只读取与 P7.8 current truth 判定有关的内容。

不要为了形式遍历整个 Repository。


======================================================================
4. 冻结 P7.8 Task Contract
======================================================================

Audit 完成后正式记录：

Task:
P7.8 — Research Result Authority & Deterministic Research Output Closure

Task Base SHA:
<TASK_BASE_SHA>

并冻结以下六项：

Goal
Modification Scope
Impact Scope
Required Behavior
Acceptance Tests
Out of Scope

这些内容构成 P7.8 Task Gate 的验收合同。


======================================================================
5. Goal
======================================================================

P7.8 的根本目标不是增加一种新的 Research 算法。

P7.8 要解决的问题是：

OnlyAlpha 当前已经存在：

Dataset Snapshot
→ Calculation Result
→ Job / Sweep orchestration
→ Feature / Factor
→ Target
→ Statistics
→ immutable Statistics Result

但仍缺少一个正式、稳定、确定性、机器可读的最终 Research output authority。

P7.8 必须建立：

Research Result

其性质必须是：

immutable
deterministic
serializable
machine-readable
verified
execution-path-neutral
physical-location-neutral

并成为未来：

Research Artifact
Query
API
Web

能够依赖的稳定只读边界。


======================================================================
6. Research Result 的第一性原理定位
======================================================================

Research Result 是：

composition authority

不是：

Calculation Result 的复制
Statistics Result 的复制
Sweep Outcome 的 durable 化
Experiment Database
Optimizer Result
Artifact
Report
Query Database

Research Result 应表达：

“一个完整研究结果由哪些已经验证的正式 Research authorities 精确组成？”

而不是：

“某次执行过程发生了什么？”


======================================================================
7. Sweep Outcome 与 Research Result 必须隔离
======================================================================

重新确认当前 Sweep Outcome contract。

如果它仍属于：

immutable ephemeral invocation evidence

则保持此边界。

以下 invocation 信息禁止进入 Research Result semantic identity：

EXECUTED
REUSED
execution duration
worker count
hostname
process id
temporary path
GitHub run id
invocation timestamp
execution order accidental detail

同一研究：

第一次 EXECUTED
第二次 REUSED

必须得到相同：

Research Result semantic identity。


======================================================================
8. Expected Modification Scope
======================================================================

这是设计阶段已经预估的修改范围。

Codex 不需要从零推导。

预计主要新增：

src/onlyalpha/research/result/**
tests/research/result/**

tests/architecture/test_research_result_boundaries.py

预计修改：

scripts/test_suite.py
scripts/verify.py

tests/architecture/test_agent_verification.py
tests/architecture/test_test_lane_contract.py
tests/architecture/test_certification_contract.py

AGENTS.md

docs/engineering/quality-system.md
docs/engineering/quality-toolchain.md

预计新增：

docs/engineering/task-gate-template.md

以及必要的：

docs/adr/**
docs/reports/**
docs/architecture.md
docs/roadmap.md
README.md
public __init__ / exports

必要时：

.github/workflows/quality.yml
.github/workflows/certification.yml

如果 Current Truth 证明某项不需要修改：

不要为了符合 Prompt 强行修改。

如果实际需要修改未列出的 production path：

必须说明：

- 为什么；
- 它属于哪个真实 Impact Scope；
- 为什么不能通过现有 public boundary 完成。


======================================================================
9. Expected Impact Scope
======================================================================

设计阶段预估 P7.8 主要影响：

A. 新的 research.result component；

B. Statistics Result public consumer boundary；

C. Dataset Snapshot identity linkage；

D. Research / Trading architecture firewall；

E. serialization / immutable result authority；

F. test lane registry；

G. Impact Resolver；

H. Task-level scoped static verification；

I. 三层验收工程约束。

正常情况下不应影响：

Trading Kernel
Broker
Position
Account
Risk
Settlement
Execution
SIM
BACKTEST
LIVE runtime implementation


======================================================================
10. 影响传播的关键规则：传播到稳定 Authority Boundary 后停止
======================================================================

不要无限反向传播测试。

错误模型：

Research Result
→ Statistics
→ Calculation
→ Dataset
→ Provider
→ entire repository

这是禁止的。

正确规则：

如果修改的是高层 Consumer：

传播到最近已经有正式 contract test 保证的 upstream authority boundary 后停止。

例如：

Research Result
→ Statistics Result public contract

正常到这里停止。

不继续机械测试：

Calculation executor
Dataset provider
Factor engine
Sweep engine

因为这些由各自 canonical contract tests 负责。


======================================================================
11. Consumer 方向传播规则
======================================================================

反过来，如果修改底层 public authority：

必须向正式 Consumer 扩张。

例如未来修改：

Statistics Result public contract

应测试：

research-evaluation
+
research-result

因为 Research Result 是正式 consumer。

如果修改：

Dataset Snapshot public identity contract

则根据实际 dependency rules，
可能扩张到：

research-dataset
research-calculation
research-evaluation
research-result

但这类扩张应由已编码的 Impact Rules 决定，
不是让 Codex 每次重新分析全仓调用图。


======================================================================
12. Expected Acceptance Plan
======================================================================

本节是 P7.8 的预估验收计划。

这是默认验收集合。

Codex 的职责是：

验证它是否仍符合 Current Truth。

不是从零重新设计测试范围。

只有满足后文 Expansion Trigger，
才允许扩大。


======================================================================
13. Block A — Result Contract / Identity
======================================================================

目标：

实现 Research Result semantic plan、identity、errors、serialization。

预计新增测试：

tests/research/result/test_plan.py
tests/research/result/test_identity.py

如果当前 Repository 命名风格更适合略有不同文件名，
允许调整。

必须覆盖：

- exact schema version；
- exact field set；
- canonical serialization；
- valid SHA256；
- invalid fingerprint fail closed；
- duplicate Statistics identity rejection；
- canonical ordering；
- input-order neutrality；
- Plan fingerprint determinism；
- Content fingerprint determinism；
- Result fingerprint determinism；
- serialization round-trip；
- unknown field fail closed；
- fresh-process identity where appropriate。

Expected pytest：

uv run pytest \
  tests/research/result/test_plan.py \
  tests/research/result/test_identity.py

Expected static：

affected Ruff
affected Format
affected Mypy

只针对新 component 及必要测试路径。

Block A 默认不运行：

research-evaluation lane
research-job lane
research-sweep lane
core-full
coverage
FULL_LOCAL


======================================================================
14. Block B — Result / Assembler
======================================================================

目标：

实现：

Research Result Manifest
Research Result immutable value
Assembler
verified upstream composition
same-Dataset enforcement

预计测试：

tests/research/result/**

至少覆盖：

- Statistics Result verified load；
- exact Statistics logical identity linkage；
- exact Statistics Result fingerprint linkage；
- canonical Statistics ordering；
- same Dataset Snapshot invariant；
- Dataset mismatch fail closed；
- Research Result Manifest exact schema；
- content fingerprint；
- result fingerprint；
- execution-path neutrality；
- fresh-process determinism；
- physical-location neutrality。

Expected pytest：

uv run pytest tests/research/result/

Expected static：

affected Ruff
affected Format
affected Mypy

Block B 默认不运行：

完整 research-evaluation lane
research-job
research-sweep
core-full
coverage
FULL_LOCAL


======================================================================
15. Block C — Immutable Research Result Store
======================================================================

目标：

实现正式 immutable Research Result authority。

预计测试继续：

uv run pytest tests/research/result/

必须新增/覆盖：

- staging write；
- reread verification；
- atomic publication；
- load_verified；
- equal recommit idempotency；
- EXECUTED / REUSED disposition；
- same identity same content reuse；
- deterministic conflict；
- own manifest corruption；
- own fingerprint corruption；
- upstream Statistics Result corruption；
- missing upstream reference；
- incorrect upstream fingerprint；
- referential integrity；
- corruption != missing；
- corrupt existing authority 不允许 overwrite；
- physical root neutrality；
- restart / fresh process reuse。

Expected static：

affected Ruff
affected Format
affected Mypy

Block C 默认不运行：

FULL_LOCAL
core-full --coverage
Final-SHA Certification


======================================================================
16. Block D — Quality Infrastructure & Documentation Closure
======================================================================

这一 Block 最后执行。

原因：

它会修改 verification infrastructure。

预期修改：

scripts/test_suite.py
scripts/verify.py

tests/architecture/test_agent_verification.py
tests/architecture/test_test_lane_contract.py
tests/architecture/test_certification_contract.py
tests/architecture/test_research_result_boundaries.py

AGENTS.md
quality-system.md
quality-toolchain.md
task-gate-template.md
ADR
roadmap
architecture
report
必要 workflow registration

Block D targeted pytest 预估：

uv run pytest \
  tests/architecture/test_agent_verification.py \
  tests/architecture/test_test_lane_contract.py \
  tests/architecture/test_certification_contract.py \
  tests/architecture/test_research_result_boundaries.py

并运行：

uv run python scripts/test_suite.py research-result

具体命令如果与当前 scripts/test_suite.py CLI 不同，
根据 Current Truth 调整。


======================================================================
17. 为什么 Block D 最后做
======================================================================

scripts/verify.py
scripts/test_suite.py
关键 verification contract tests

属于：

VERIFICATION_INFRASTRUCTURE

修改验证基础设施必须 fail closed。

因此最终会触发一次：

FULL_LOCAL

如果过早修改 verification infrastructure，
A / B / C 每一小节都会不断触发大范围验收。

这是低效的。

正确实施顺序：

A targeted
↓
B targeted
↓
C targeted
↓
D verification infrastructure
↓
FULL_LOCAL once
↓
P7.8 Task Gate

不要：

A FULL_LOCAL
B FULL_LOCAL
C FULL_LOCAL
D FULL_LOCAL


======================================================================
18. Expected Canonical Lane
======================================================================

P7.8 新增：

research-result

canonical lane。

它的 semantic ownership 应主要是：

tests/research/result/**
tests/architecture/test_research_result_boundaries.py

不要为了“保险”把：

research-job
research-sweep
research-evaluation
research-calculation
research-dataset

整个测试目录塞进 research-result lane。

如果 Research Result integration tests 需要真实 Statistics authority，
应通过 tests/research/result 自己的 integration fixture
直接使用正式 public contract。


======================================================================
19. Research Result Plan
======================================================================

根据当前 Repository 风格设计最小明确 Plan。

如果没有更好的现有模式冲突，建议类似：

OnlyResearchResultPlan

它声明：

statistics_fingerprints

约束：

- 至少一个；
- exact SHA256；
- duplicate fail closed；
- semantic order independent；
- canonical sort；
- schema versioned；
- exact serialization；
- unknown field fail closed。

禁止以下 presentation 信息进入 Plan identity：

name
title
display_order
description
tag
UI label


======================================================================
20. Identity 分层
======================================================================

必须区分至少三个概念：

A. Research Result Plan Fingerprint

表达：

“要什么研究结果？”

由：

schema version
+
canonical Statistics logical identities

产生。

B. Research Result Content Fingerprint

Assembler 对每个：

statistics_fingerprint

执行正式：

StatisticsResultStore.load_verified(...)

得到 exact：

statistics_result_fingerprint

然后构造 canonical：

statistics_fingerprint
→
statistics_result_fingerprint

verified content references。

这些 references 产生：

research_result_content_fingerprint

C. Research Result Fingerprint

由：

plan fingerprint
+
content fingerprint

产生最终：

research_result_fingerprint


======================================================================
21. Semantic Identity 中禁止出现的内容
======================================================================

以下内容不得进入 Research Result semantic identity：

created_at
physical root
filesystem path
temporary path
compression
JSON whitespace
hostname
process id
worker id
execution duration
EXECUTED
REUSED
GitHub run
display alias
description
tag

created_at 可以保留为 provenance，
但不参与 semantic fingerprint。


======================================================================
22. Dataset Snapshot V1 不变量
======================================================================

P7.8 V1 默认要求：

一个 Research Result 中所有 Statistics Results
必须属于同一个 exact Dataset Snapshot。

合法：

S1 -> D1
S2 -> D1

非法：

S1 -> D1
S2 -> D2

非法情况：

explicit fail closed。

不要实现：

automatic cross-dataset merge
nearest join
timestamp coincidence
fuzzy alignment
implicit normalization

如果未来需要 multi-Dataset comparison：

另建显式 semantic contract。


======================================================================
23. Research Result 不复制 Statistics 数据
======================================================================

禁止：

Statistics Result rows
↓ copy
Research Result rows

Research Result 只保存精确引用。

权威关系：

Statistics Result Store
拥有 Statistics data

Research Result
拥有 composition/provenance authority

未来 Artifact / Query 如果需要数据：

Research Result
↓ verified references
Statistics Result Store
↓
read


======================================================================
24. Manifest
======================================================================

根据 Repository 当前模式设计 exact/versioned manifest。

预计包含类似：

schema_version

plan
research_result_plan_fingerprint

dataset_snapshot_fingerprint

statistics_results:
    statistics_fingerprint
    statistics_result_fingerprint

research_result_content_fingerprint
research_result_fingerprint

created_at

不要机械照抄上述字段名。

先检查当前 Result Store conventions。

但 semantic 分层必须保持。


======================================================================
25. Assembler
======================================================================

Assembler 的职责只有：

Plan
↓
verified Statistics Results
↓
exact identity validation
↓
same Dataset validation
↓
canonical references
↓
content identity
↓
Research Result

Assembler 不负责：

raw Dataset loading
Calculation execution
Factor execution
Target execution
Statistics calculation
Job execution
Sweep execution
Optimizer selection


======================================================================
26. Result Store
======================================================================

Research Result Store 必须遵循正式 authority 模式：

build candidate
↓
staging write
↓
read back
↓
verify exact content
↓
atomic publication
↓
verified load

要求：

equal recommit
→ idempotent REUSED

same semantic authority
different deterministic content
→ DETERMINISTIC_RESULT_CONFLICT

existing corrupt authority
→ fail closed

禁止：

delete corrupt result
overwrite corrupt result
corruption treated as NOT_FOUND
silent rebuild over corrupt authority


======================================================================
27. load_verified 必须做 Referential Integrity
======================================================================

Research Result 自己写：

S1 -> SR1

并不等于可信。

load_verified 必须验证：

StatisticsResultStore.load_verified(S1)
→ actual Statistics Result identity == SR1

所有 referenced Statistics Result
必须逐一 verified。

如果 upstream：

missing
corrupt
identity mismatch
dataset mismatch

Research Result load 必须 fail closed。


======================================================================
28. 建议代码结构
======================================================================

先查看现有风格。

如果没有冲突，预计：

src/onlyalpha/research/result/
    __init__.py
    errors.py
    plan.py
    identity.py
    result.py
    assembler.py
    result_store.py

不要为了文件对称性机械创建全部文件。

职责保持单一。

禁止无必要新增：

ResearchResultManager
ResearchResultService
ExperimentManager
ResearchDatabase
GenericRepositoryFramework


======================================================================
29. Architecture Firewall
======================================================================

Research Result 可以依赖：

research.evaluation 的正式 public authority
必要 dataset identity helper
正式 deterministic serialization / hash helper

禁止依赖：

runtime.trading
order
position
account
risk
broker
execution
settlement
Web
UI
CLI presentation
mutable Sweep invocation state

新增：

tests/architecture/test_research_result_boundaries.py

长期锁定这些关系。


======================================================================
30. Expected Explicit Non-Requirements
======================================================================

下面这些默认明确不属于 P7.8 Task Gate：

research-dataset full lane
research-calculation full lane
research-factor full lane
research-job full lane
research-sweep full lane
research-evaluation full lane

recovery
sim-recovery
ashare
miniqmt-contract

core-full --coverage
all Research coverage
repository-wide coverage
Nightly Heavy Quality
Final-SHA Certification

不要为了保险执行。

只有 Expansion Trigger 明确成立时，
才允许加入对应验证。


======================================================================
31. Expansion Triggers
======================================================================

只有以下情况允许扩大预估 Acceptance Plan。

----------------------------------------------------------------------
Trigger A — 实际修改 Statistics public contract
----------------------------------------------------------------------

例如修改：

src/onlyalpha/research/evaluation/**
Statistics Result Manifest
Statistics Result identity
Statistics Result Store public behavior

则追加：

对应 research-evaluation affected tests
必要时 research-evaluation canonical lane

因为这是 producer contract modification。


----------------------------------------------------------------------
Trigger B — 修改 Dataset / Calculation public identity
----------------------------------------------------------------------

如果 P7.8 被迫修改：

Dataset Snapshot identity
Calculation Result identity
公共 serialization contract

则按 scripts/verify.py 的 consumer dependency
扩展到最近稳定消费者边界。

不要手工猜整个 Repository。


----------------------------------------------------------------------
Trigger C — 修改 Job / Sweep public contract
----------------------------------------------------------------------

只有如果 P7.8 实际修改：

research/job/**
research/sweep/**

才追加对应 lane。

Research Result 本身不应因为概念上位于 Sweep 后面，
就默认运行 Sweep lane。


----------------------------------------------------------------------
Trigger D — package / build metadata
----------------------------------------------------------------------

如果修改：

pyproject.toml
entry point
workspace dependency
distribution metadata
version metadata

加入：

targeted build
version synchronization checks


----------------------------------------------------------------------
Trigger E — architecture boundary
----------------------------------------------------------------------

如果修改 import/layering/public package direction：

加入：

Import Linter
对应 architecture tests


----------------------------------------------------------------------
Trigger F — unknown path
----------------------------------------------------------------------

无法分类的真实 modification：

fail closed

→ FULL_LOCAL


----------------------------------------------------------------------
Trigger G — verification infrastructure
----------------------------------------------------------------------

修改：

scripts/verify.py
scripts/test_suite.py
scripts/certification.py
关键 verification contract tests
关键 workflow

→ VERIFICATION_INFRASTRUCTURE
→ FULL_LOCAL


======================================================================
32. Codex 不允许主动扩大测试范围的理由
======================================================================

不要因为：

“可能还有影响”
“多跑一些更保险”
“CI 反正会跑”
“测试不贵”

就扩大 Task Gate。

如果认为需要追加测试，
必须明确给出：

Changed Contract:
<什么被修改>

Consumer:
<谁真正消费>

Why Existing Boundary Is Insufficient:
<为什么预估验收不能证明正确>

Additional Verification:
<追加什么>

如果无法说明：

不追加。


======================================================================
33. 三层验收模型
======================================================================

本任务必须将 OnlyAlpha 的正式质量模型长期固化为：

Task Gate
Phase Gate
Certification Gate

仅此三层。

不要创造新的正式：

Subtask Gate
Coverage Gate
Integration Gate
Remote Gate

Block A/B/C/D 只是 Implementation Blocks，
不是新的工程状态。


======================================================================
34. Task Gate
======================================================================

Task Gate 回答：

“本次明确修改及真实 Impact Scope 是否正确？”

每个非微小 Task 开始必须冻结：

TASK_BASE_SHA
Goal
Modification Scope
Impact Scope
Required Behavior
Expected Acceptance Tests
Out of Scope
Expansion Triggers

Task Gate 的测试原则：

最小
但必须充分。

不是：

最少测试。

也不是：

最多测试。

而是：

能够覆盖真实 affected contracts 的最小稳定集合。


======================================================================
35. Phase Gate
======================================================================

P7.8 Task Complete
不等于：

P7 Phase Complete。

只有 P7 全部正式 semantic increments 完成后，
才执行一次 P7 Phase Gate。

Phase Gate 才默认包含：

repository-wide static
全部相关 canonical regression lanes
architecture/conformance
full required branch coverage
workspace build
P7 functional end-to-end scenario

这里才应该出现类似：

uv run python scripts/test_suite.py core-full --coverage

不要把 Phase Gate 下放给 P7.8 Block。


======================================================================
36. Certification Gate
======================================================================

P7 Phase Gate PASS 后：

freeze immutable Final SHA

然后正式：

Final-SHA Certification

Certification 包括自己的 mandatory matrix。

Task Impact Resolver：

永远不能缩小 Certification Gate。

必须通过 architecture/contract test 锁死。


======================================================================
37. CI 与 Task Acceptance 分离
======================================================================

GitHub workflow 可以比普通 Task Gate 更广。

这不代表：

Codex 必须等待整个 CI
才能判断 Task Complete。

正式规则：

CI pending != Task blocked

但是：

如果远端已经明确发现当前修改导致真实 regression：

必须修复。

不得以：

“本地 Task Gate 已 PASS”

忽略已知错误。


======================================================================
38. scripts/verify.py 的目标
======================================================================

P7.8 必须让 verify.py 真正服务于：

actual diff
→ deterministic Impact Scope
→ minimal sufficient verification

而不是：

actual diff
→ almost always repository-wide verification

至少新增：

research-result Impact Rule。

正常：

src/onlyalpha/research/result/**
tests/research/result/**

→ research-result lane

Statistics public contract modification：

→ research-evaluation
+
research-result

unknown：

→ FULL_LOCAL

verification infrastructure：

→ FULL_LOCAL


======================================================================
39. Scoped Static Verification
======================================================================

重新检查当前 verify.py。

如果 component task 仍机械执行整套：

repository-wide Ruff
repository-wide Mypy
all packages type checks
version checks

则需要修正。

Task pytest 已经 scoped，
static verification 也必须 scoped。


======================================================================
40. Expected Scoped Static Model
======================================================================

可以设计类似：

VerificationStaticPlan

包含：

ruff_targets
format_targets
mypy_targets
import_linter_required
version_sync_required
build_targets

不要为了实现这个模型引入复杂框架。

核心行为即可。


======================================================================
41. Ruff
======================================================================

普通 Research Result modification：

Ruff 只检查 affected：

src/onlyalpha/research/result/**
tests/research/result/**

以及必要 architecture test。

Phase/CI 仍可运行 full Ruff。


======================================================================
42. Mypy
======================================================================

普通 component modification：

检查 affected typed module。

如果 public API 改动：

增加对应 typed consumer。

禁止普通 Task 默认检查：

全部 market package
全部 provider package
全部 plugin package

除非真实 Impact Scope 包含它们。


======================================================================
43. Import Linter
======================================================================

只有：

package layering
Research/Trading firewall
Plugin/Core direction
architecture boundary

变化时才进入 Task Gate。

普通 algorithm/private helper modification
不自动运行。


======================================================================
44. Build
======================================================================

只有修改：

build metadata
package metadata
entry point
workspace dependencies
distribution layout

才默认加入 Task build。

普通 `.py` implementation change
不要求 whole-workspace build。

注意：

P7.8 最终因为 verification infrastructure integration
可能由 FULL_LOCAL 包含 build。

不要因此每个 Block 都 build。


======================================================================
45. Version Sync
======================================================================

只有真实 version/package metadata modification
才加入 Task-level version sync。

不要每次 ordinary Task 都机械执行。


======================================================================
46. AGENTS.md 长期固化
======================================================================

AGENTS.md 是 Agent / Codex 顶层工程执行合同。

必须新增或完善简洁的：

Quality Acceptance Contract

明确：

OnlyAlpha 只有：

Task Gate
Phase Gate
Certification Gate

普通 implementation task：

默认 Task Gate。

开始前必须读取：

TASK_BASE_SHA
Goal
Modification Scope
Impact Scope
Required Behavior
Expected Acceptance Tests
Out of Scope
Expansion Triggers

并引用：

docs/engineering/quality-system.md
= normative semantics

docs/engineering/quality-toolchain.md
= execution mechanics

不要复制完整质量文档。


======================================================================
47. AGENTS.md 必须禁止的行为
======================================================================

明确写入：

普通 Task 不得仅为了保险默认执行：

release
core-full --coverage
repository-wide coverage
Nightly
Final-SHA Certification

除非真实 Impact Scope、
verification infrastructure
或当前 Gate 类型明确要求。


======================================================================
48. quality-system.md
======================================================================

继续作为：

验收语义唯一 normative authority。

不要新增第二份 quality policy。

检查并补足：

A. 三层 Gate 唯一性；

B. Task Contract 必须提前冻结；

C. TASK_BASE_SHA；

D. Expected Acceptance Plan；

E. Expansion Triggers；

F. Task static checks 同样 Impact-aware；

G. consumer impact；

H. stable authority boundary stop rule；

I. implementation block != Gate；

J. Agent/Prompt 不得定义冲突质量规则。


======================================================================
49. quality-toolchain.md
======================================================================

继续作为：

工具映射与执行 authority。

明确 ordinary task workflow：

Freeze Task Contract
↓
record TASK_BASE_SHA
↓
implement
↓
run Expected Acceptance Tests
↓
derive actual changed set
↓
verify.py checks expected impact
↓
only evidence-based expansion
↓
Task Complete

明确：

coverage 默认属于：
Phase / Certification

不是普通 Task。


======================================================================
50. task-gate-template.md
======================================================================

新增一个短模板。

它不是规范 authority。

建议结构：

# Task Contract

Task:
Base SHA:

Goal:

Expected Modification Scope:

Expected Impact Scope:

Required Behavior:

Expected Acceptance Tests:

Expected Canonical Lanes:

Expected Static Checks:

Explicit Non-Requirements:

Expansion Triggers:

Out of Scope:


# Completion Evidence

Actual Changed Scope:

Actual Impact Scope:

Expected Tests Executed:

Additional Tests Executed:

Reason For Expansion:

Static Verification:

Explicitly Not Executed:

Verdict:


======================================================================
51. test_agent_verification.py
======================================================================

自动锁定至少以下行为：

1. research-result modification
   → research-result

2. research-result modification
   不自动：
   → all research
   → core-full

3. Statistics public authority modification
   → research-evaluation
   + research-result

4. docs-only
   → no runtime lane

5. unknown path
   → FULL_LOCAL

6. verification infrastructure
   → FULL_LOCAL

7. Impact union 只能增加不能缩小

8. component static
   → scoped

9. package metadata
   → required targeted build/version checks

10. unrelated package
    → not selected

11. architecture boundary modification
    → corresponding architecture check


======================================================================
52. test_test_lane_contract.py
======================================================================

新增并锁定：

research-result

检查：

- canonical lane name；
- correct test roots；
- architecture boundary test；
- release/phase registration where appropriate；
- coverage registration where appropriate；
- deterministic CLI behavior。


======================================================================
53. test_certification_contract.py
======================================================================

明确测试：

Task Impact Resolver
不能裁剪：

Certification mandatory lanes
Certification coverage
Certification build
Certification security
Certification Final SHA behavior

Task verification 与 Certification
必须保持架构隔离。


======================================================================
54. P7.8 Coverage
======================================================================

可以注册：

research-result --coverage

并根据现有 Research durable authority 标准设置合理 threshold。

如果当前架构一致，建议目标：

line >= 95%
branch >= 90%

但是：

不要在 P7.8 Block A/B/C 默认运行 coverage。

不要在 P7.8 Task Gate 默认因为有 coverage lane
就执行 coverage。

它主要供：

P7 Phase Gate
Certification Gate

使用。


======================================================================
55. P7.8 Expected Pytest Plan
======================================================================

默认计划如下。

--------------------------------------------------
Block A
--------------------------------------------------

uv run pytest \
  tests/research/result/test_plan.py \
  tests/research/result/test_identity.py

--------------------------------------------------
Block B
--------------------------------------------------

uv run pytest tests/research/result/

--------------------------------------------------
Block C
--------------------------------------------------

uv run pytest tests/research/result/

--------------------------------------------------
Block D targeted
--------------------------------------------------

uv run pytest \
  tests/architecture/test_agent_verification.py \
  tests/architecture/test_test_lane_contract.py \
  tests/architecture/test_certification_contract.py \
  tests/architecture/test_research_result_boundaries.py

--------------------------------------------------
P7.8 canonical semantic lane
--------------------------------------------------

uv run python scripts/test_suite.py research-result

--------------------------------------------------
P7.8 final verification infrastructure closure
--------------------------------------------------

FULL_LOCAL exactly once

使用当前 verify.py / test_suite.py 的正式命令。

如果当前 CLI 与上述表达不同：

只调整命令形式，
不改变验收语义。


======================================================================
56. 默认不执行的 pytest / lanes
======================================================================

除非 Expansion Trigger 成立：

不要执行：

research-dataset
research-calculation
research-factor
research-job
research-sweep
full research-evaluation
recovery
sim-recovery
ashare
miniqmt-contract

更不要因为高层 Result 依赖 Statistics
就逆向一路跑完整 Research stack。


======================================================================
57. Codex 的测试判断职责已经被限制
======================================================================

Codex 不负责：

“从整个 Repository 从零决定应该跑哪些 pytest。”

Codex 只负责判断：

1. Expected tests 是否仍存在/合理；
2. Current Truth 是否新增直接正式 consumer；
3. Actual Changed Scope 是否超过 Expected Modification Scope；
4. 是否修改 public authority；
5. 是否触发 Expansion Trigger；
6. verify.py 是否发现遗漏影响。

默认：

Expected Acceptance Plan
就是执行计划。


======================================================================
58. Audit 后必须输出 Acceptance Delta
======================================================================

Current Truth Audit 完成后，
在写代码前输出简洁的：

Acceptance Plan Validation

例如：

Expected:
research-result tests
Status:
valid

Expected:
research-evaluation full lane not required
Status:
valid because Evaluation public contract unchanged

Expected:
research-sweep not required
Status:
valid because Result does not consume Sweep invocation authority

Expected:
FULL_LOCAL once
Status:
required because Block D modifies verification infrastructure

只报告差异。

不要重新长篇解释整个验收哲学。


======================================================================
59. P7.8 最终正式 Task Gate
======================================================================

Block A/B/C/D 全部完成后，
P7.8 只产生一个正式 Task Gate Verdict。

必须确认：

A. Research Result semantic tests PASS；

B. research-result canonical lane PASS；

C. Research Result architecture boundary PASS；

D. agent verification contract PASS；

E. lane contract PASS；

F. certification contract PASS；

G. affected static verification PASS；

H. FULL_LOCAL PASS once；

I. actual diff 已重新检查；

J. actual Impact Scope 没有遗漏；

K. documents current truth 已同步。

默认明确不需要：

core-full --coverage
repository-wide coverage
Nightly Heavy Quality
Final-SHA Certification


======================================================================
60. FULL_LOCAL 不等于 Phase Gate
======================================================================

因为 P7.8 修改 verification infrastructure：

最终 Task Gate 运行一次 FULL_LOCAL。

这只是：

Task-level self-verification firewall。

它不等于：

P7 Phase Gate。

不要因为已经 FULL_LOCAL，
就继续：

core-full --coverage
all branch coverage
Final-SHA Certification。


======================================================================
61. Documentation Truth
======================================================================

P7.8 完成后检查：

README.md
docs/architecture.md
docs/roadmap.md
AGENTS.md

确保：

Research Result 如果真实实现，
文档不再写“尚未实现”。

但不得提前宣布：

Research Artifact complete
Query/API complete
Web complete
Research Runtime active
P7 Phase Complete
P7 Certified


======================================================================
62. ADR
======================================================================

根据当前 ADR 编号选择正确下一号。

不要盲目假定编号。

ADR 应记录：

Research Result Authority
Identity layering
Composition-only ownership
Statistics reference integrity
same Dataset invariant
execution-path neutrality
immutable Store
corruption/conflict
Research/Trading firewall
stable authority boundary

Rejected Alternatives 至少讨论：

- copy Statistics rows；
- Sweep Outcome as Research Result；
- mutable Experiment DB as primary authority；
- EXECUTED/REUSED in semantic identity；
- physical path in identity；
- automatic cross-Dataset merge；
- Web 直接读取内部 stores；
- Result 依赖 Trading authority。


======================================================================
63. Implementation Report
======================================================================

新增 P7.8 report。

报告必须记录真实 evidence。

包括：

Task Base SHA
actual final implementation SHA if available
actual Modification Scope
actual Impact Scope
expected Acceptance Plan
actual tests executed
additional tests executed
reason for expansion
FULL_LOCAL command/result
checks explicitly not executed
remote CI state if known
remaining P7 scope

不得伪造：

PASS
VERIFIED
CERTIFIED
ACCEPTED

如果实际证据不存在。


======================================================================
64. 不要为了测试改变正确架构
======================================================================

测试失败时先分类：

production regression
stale test
incorrect fixture
wrong expected behavior
architecture drift
verification-tool defect
serialization nondeterminism
actual public-contract break

只有生产语义确实错误时才改变 production behavior。

不要恢复 legacy behavior
只为了让旧 test PASS。


======================================================================
65. Definition of Done
======================================================================

只有下面全部真实满足，
才能将 P7.8 标为 Task Complete / VERIFIED：

1. 正式 Research Result authority 已实现；

2. Research Result 不复制 Statistics authority；

3. Plan / Content / Result identity 分层明确；

4. exact schema/versioning 正确；

5. canonical ordering 正确；

6. fresh-process determinism 正确；

7. physical-location neutrality 正确；

8. execution-path neutrality 正确；

9. same Dataset invariant 正确；

10. verified upstream referential integrity 正确；

11. immutable Store 正确；

12. equal recommit idempotent；

13. deterministic conflict fail closed；

14. corruption fail closed；

15. Research/Trading architecture firewall 正确；

16. research-result canonical lane 建立；

17. expected pytest plan 建立并通过；

18. consumer propagation rules 正确；

19. stable authority boundary stop rule 被实现/记录；

20. scoped Task static verification 正确；

21. unknown impact 仍 fail closed；

22. verification infrastructure self-change 仍 FULL_LOCAL；

23. Certification 不可被 Task Resolver 裁剪；

24. AGENTS.md 固化三层 Gate；

25. quality-system.md 成为稳定 normative authority；

26. quality-toolchain.md 明确实际执行方式；

27. task-gate-template.md 建立；

28. P7.8 FULL_LOCAL 最终一次 PASS；

29. current-truth documentation 同步；

30. 未错误运行/要求 P7 Phase/Certification Gate。


======================================================================
66. P7.8 完成以后工程应该达到的状态
======================================================================

产品层：

Dataset Snapshot
        ↓
Calculation Result
        ↓
Feature / Target
        ↓
Statistics Result
        ↓
Research Result
        ↓
future Artifact / Query / API / Web

Research Result 成为稳定 Research output boundary。


质量层：

Task Design
        ↓
Expected Impact Scope
        ↓
Expected Acceptance Tests
        ↓
Codex one-time Current Truth validation
        ↓
Implementation
        ↓
targeted tests
        ↓
verify.py actual-diff safeguard
        ↓
Task Complete

而不是：

每一小节
↓
重新阅读整个 Repository
↓
重新推导全部 tests
↓
全仓 pytest
↓
full coverage
↓
等所有 CI
↓
下一小节


======================================================================
67. 最终报告格式
======================================================================

最终只输出以下结构。

1. Current Truth
   - base branch
   - TASK_BASE_SHA
   - resulting HEAD
   - version
   - P7 state

2. Acceptance Plan Validation
   - expected plan
   - deviations
   - reasons

3. Root Problem
   - Research Result 解决什么
   - 为什么不是 Sweep Outcome / Statistics Result

4. Architecture Implemented
   - authority
   - identity
   - immutable storage
   - verified references
   - failure semantics

5. Actual Changed Scope

6. Actual Impact Scope

7. Verification
   - Block A command/result
   - Block B command/result
   - Block C command/result
   - Block D command/result
   - research-result lane
   - final FULL_LOCAL

8. Expansion
   - additional verification
   - exact reason
   - or "None"

9. Explicitly Not Executed
   - full coverage
   - Phase Gate
   - Certification Gate
   - other non-required lanes

10. Documentation Updated

11. Remaining P7 Scope

12. Verdict
   - P7.8 Task Complete / not complete
   - do not claim P7 completion unless independently true


======================================================================
68. 最终原则
======================================================================

不要以：

“跑了更多测试”

衡量质量。

也不要以：

“跑了更少测试”

衡量效率。

正确目标是：

真实 Impact Scope
+
最小充分 Verification
+
稳定 Authority Boundary
+
机器化 fail-closed 兜底

设计阶段负责预估绝大多数验收范围。

Codex 负责：

验证预估
实施
发现真实差异
有证据地扩大

verify.py 负责：

根据 actual diff
做最后机器化检查。

三者不是重复从零分析，
而是：

设计预估
        ↓
Codex 校正
        ↓
机器兜底

始终从第一性原理解决问题。

不要用流程复杂度替代工程判断。
不要用全仓测试替代影响分析。
不要用减少测试换取速度。

通过更精确的验收边界，同时获得：

正确性
+
确定性
+
更少 token
+
更短反馈周期
+
更少重复验收
+
更可靠的长期工程约束。