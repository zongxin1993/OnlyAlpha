你现在负责实现 OnlyAlpha P8.0 — Research Specification & Resolution Boundary。

Repository:
https://github.com/zongxin1993/OnlyAlpha

============================================================
0. 工作方式与最高原则
============================================================

不要直接按照本 Prompt 中猜测的文件结构机械实现。

第一步必须重新阅读当前 Repository HEAD，理解当前实现后再开始修改。

Repository 当前 HEAD、代码、测试、ADR、README、docs/roadmap.md、质量工具是 current truth。
历史 Prompt 和本 Prompt 中的具体类名/目录建议，在与 current truth 冲突时只能作为设计意图参考。

开始时执行并记录：

TASK_BASE_SHA="$(git rev-parse HEAD)"

同时确认：
- git status
- README.md
- docs/roadmap.md
- docs/engineering/quality-system.md
- docs/engineering/quality-toolchain.md
- P7/P8 相关 ADR
- P7 Research Job / Sweep / Statistics / Result / Runtime implementation
- Calculation Definition / Graph / Registry
- 当前 canonical serialization/fingerprint infrastructure
- tests/research/**
- tests/runtime/research/**
- scripts/test_suite.py
- scripts/verify.py
- CI / canonical lane configuration

任何修改前先形成 Task Gate：

Goal
Modification Scope
Impact Scope
Required Behavior
Expected Acceptance Tests
Expansion Triggers
Out of Scope

必须遵守 OnlyAlpha 工程原则：

Correctness
>
Architecture Consistency
>
Verifiability
>
Recoverability
>
Maintainability
>
Performance
>
Automation

并继续遵守：

- Repository = current truth
- One Domain = One Write Authority
- Planner Calculates → Projection Installs
- Commit Fact First → Project State Second
- Historical Fact Immutable → Forward Recovery Only
- Unsupported / Ambiguous → Fail Closed
- No hidden fallback
- No second economic truth
- Research 优化 Research execution efficiency
- Backtest / Sim / Live 共享 Trading semantic core
- Runtime Type != Execution Permission

本任务要求从第一性原理解决问题。

不要为了快速满足测试：
- 创建重复模型
- 创建 Web-specific semantic DTO
- 复制 Sweep materialization 算法
- 增加 hidden fallback
- 增加 latest/fuzzy lookup
- 通过特殊 case 拼装 fingerprint
- 让 API/Web/Pydantic 成为 Core authority

代码应保持模块职责单一、命名明确、依赖方向清晰。

============================================================
1. P8.0 根本问题
============================================================

P7 已经建立：

Dataset Snapshot
→ Calculation
→ Research Job / Sweep
→ Statistics
→ Research Result
→ Artifact
→ Query/API/Web
→ finite Research Runtime

当前真正缺失的是：

External User Intent
        ↓
Portable Versioned Research Specification
        ↓
Deterministic Resolution
        ↓
Existing OnlyResearchWorkloadPlan

当前 Research Workload 主要由 Python 手工构造：

OnlyResearchJobPlan
OnlyResearchSweepPlan
OnlyResearchStatisticsPlan
OnlyResearchResultPlan
OnlyResearchWorkloadPlan

这对 Core 是合理的，
但不能成为未来 Web / HTTP / Scheduler / durable Research Run 的输入协议。

P8.0 的目标不是再创造一套 Research semantic model。

P8.0 要建立：

Portable User Intent
        ↓
Canonical Research Specification
        ↓
Deterministic Compiler / Resolver
        ↓
Existing P7 exact semantic contracts
        ↓
OnlyResearchWorkloadPlan

P8.0 本质上是：

Research Compiler Front Door

而不是：
- Web
- HTTP API
- Scheduler
- Worker
- PostgreSQL
- Research Run

============================================================
2. 最重要的长期目标：
   Research → Backtest → Sim → Live 不发生语义漂移
============================================================

P8.0 必须为未来以下产品链打好基础：

Web Research × N
        ↓
选择有价值的 Research Candidate
        ↓
Promote / Select to Backtest
        ↓
Backtest
        ↓
Sim
        ↓
Live

关键原则：

不要设计：

Research Strategy
    ↓ convert
Backtest Strategy
    ↓ convert
Sim Strategy
    ↓ convert
Live Strategy

这是错误模型。

正确模型是：

                Canonical Semantic Definition
                         │
             ┌───────────┼───────────┐
             ▼           ▼           ▼
          Research    Backtest    Sim / Live
          Backend     Backend      Backend

Runtime 可以不同。

Execution backend 可以不同。

但是 canonical semantics 必须保持一致。

============================================================
3. 极重要的 identity 边界
============================================================

重新确认当前代码后再实现以下不变量。

Research 的 calculation_fingerprint 是 Dataset-bound identity。

逻辑上类似：

Research Calculation Identity
=
Dataset Snapshot Fingerprint
+
Calculation Graph Fingerprint

因此：

research_calculation_fingerprint

不能成为未来 Research → Backtest promotion 的跨 Runtime identity。

例如：

Dataset A + Graph G → Research Calculation A
Dataset B + Graph G → Research Calculation B

A != B

这是正确行为。

真正应跨 Runtime 保持的 semantic identity 是：

OnlyCalculationGraphDefinition
+
calculation_graph.fingerprint

也就是说：

Research:
Dataset X
+
Graph G
→ Research calculation identity RX

Future Backtest:
Backtest environment/data
+
Graph G
→ Trading execution

Graph G 必须保持 exact identity。

未来核心 invariant：

selected Research candidate graph fingerprint
==
Backtest strategy calculation graph fingerprint

如果不相等：
FAIL CLOSED

P8.0 不需要实现 Backtest promotion，
但必须让所有 resolved candidate 都能够保留这个 exact graph identity 和 provenance。

============================================================
4. Candidate semantics 与 Research evaluation context 必须分离
============================================================

Research Specification 中虽然可以整体表达：

Dataset
Calculation
Sweep
Target
Statistics

但架构上必须理解为两类信息。

A. Candidate Semantic Space

例如：

Momentum
period = 10 / 20 / 40

它描述：
“什么 semantic candidate 值得研究”。

核心是 runtime-neutral：

Calculation Type
Calculation Graph
Parameters
Sweep Assignment

B. Research Evaluation Context

例如：

Dataset Snapshot X
Forward Return 5D
IC
Rank IC

它描述：
“如何在 Research 中评价 Candidate”。

Dataset / Target / Statistics 不应该被错误提升到未来 Backtest Strategy。

未来：

Research Specification
        │
        ├── Candidate semantics ───→ Future Strategy Composition
        │
        └── Evaluation context
             Dataset
             Target
             Statistics
                   X
                   不 promotion

P8.0 不需要创建完整 Strategy Definition。

但是：

P8.0 candidate semantics MUST be promotion-ready and runtime-neutral.

============================================================
5. Specification 不是第二套 semantic authority
============================================================

必须冻结：

Research Specification
!=
Research Result

Research Specification
!=
Calculation Definition

Research Specification
!=
Calculation Graph identity

Research Specification
!=
Research Job identity

Research Specification
!=
Runtime State

Research Specification 是：

“用户要求 OnlyAlpha 执行什么 Research”
的 canonical versioned immutable document。

Specification fingerprint 只代表：

canonical request document identity

它可以用于未来：
- submission audit
- Research Run reference
- duplicate request observation
- logging
- Web display

它绝不能替代：

calculation_fingerprint
statistics_fingerprint
research_result_fingerprint
artifact identity

Existing semantic authorities 保持不变。

============================================================
6. Core Specification V1
============================================================

根据当前 Repository 重新评估具体命名。

推荐模型方向：

OnlyResearchSpecification

至少包含：

- schema_version
- dataset_snapshot_fingerprint
- calculations
- statistics

不要包含：

- Run ID
- Runtime ID
- Engine ID
- Worker ID
- User ID
- submitted_at
- created_at
- user_data_root
- hostname
- browser metadata
- database metadata
- execution state

这些属于未来 P8.1 Operational Plane。

推荐：

OnlyResearchCalculationSpec

表达：

- calculation_id
- graph_template
- sweep_dimensions

calculation_id 是 Specification-local symbolic identity。

例如：

feature
target
momentum_candidate

它不是 semantic fingerprint。

============================================================
7. Dataset 设计
============================================================

Specification V1 只允许一个 exact Dataset Snapshot：

dataset_snapshot_fingerprint

必须：
- lower-case SHA256
- exact
- immutable reference

禁止：

latest
current
today
production
dataset alias
fuzzy name lookup
hidden catalog resolution

当前 OnlyResearchWorkloadPlan 本来就要求所有 Research calculation 使用同一个 Dataset Snapshot。

因此 Dataset 应在顶层表达一次。

不要在：
- feature
- target
- sweep
- statistics

重复声明 Dataset。

P8.0 Resolver 只需要验证：
- fingerprint format
- internal semantic consistency

不要让纯 Specification Compiler 强依赖 filesystem。

Dataset 是否真实存在以及 load_verified()，
留给未来 P8.1 Submission Admission / Runtime defensive loading。

保持：

P8.0 = pure deterministic compilation

P8.1 = durable admission / authority availability

============================================================
8. Graph Template
============================================================

当前 P7 Sweep 已经存在：

OnlyResearchGraphTemplate
OnlyResearchGraphTemplateNode
OnlyResearchTemplateReference
OnlyResearchTemplateInputBinding

它们已经具备：
- exact CalculationTypeReference
- symbolic template_node_id
- typed parameters
- explicit dependencies
- source references
- strict schema
- cycle validation
- canonical ordering
- to_dict/from_dict

不要重造：

WebGraph
SpecificationGraph
ResearchGraphV2

只允许一套 Graph Template authority。

当前 GraphTemplate 如果仍位于：

research.sweep.template

需要从 first principles 判断 ownership 是否已经错误。

P8.0 后：
- Direct Calculation Specification 使用它
- Sweep 使用它
- Specification Resolver 使用它

因此它本质上已经属于：

Research Calculation pre-materialization boundary

推荐 canonical ownership：

onlyalpha.research.calculation.template

如果迁移：
- 只能存在一个正式实现
- 可以根据现有 public API 需要保留 re-export
- 不允许复制两份 class/validation logic
- 不要形成两个 GraphTemplate authority

============================================================
9. 抽取唯一 Graph Template Materializer
============================================================

当前 Sweep Planner 内部已有 materialization 逻辑。

它负责：

Graph Template
+
parameters
+
symbolic dependency
        ↓
Calculation Registry
        ↓
type-owned Definition resolver
        ↓
canonical Calculation Definition
        ↓
OnlyCalculationGraphDefinition

P8.0 Direct Calculation 也需要完全相同逻辑。

禁止复制。

应抽取唯一：

OnlyResearchGraphTemplateMaterializer

名称可根据 current truth 调整。

推荐 ownership：

onlyalpha.research.calculation

Sweep Planner 和 Specification Resolver 都必须复用它。

最终必须只有一套：

parameter normalization
dependency resolution
Definition rematerialization
canonical graph construction

============================================================
10. Materialization 必须保留 symbolic → exact provenance
============================================================

P8.0 后 future Web/Promotion 必须知道：

template_node_id
        ↓
resolved node_fingerprint

因此 Materializer 不应该只返回 Graph。

建议形成一个 ephemeral derived value，例如：

OnlyResearchGraphMaterialization

包含：

- graph: OnlyCalculationGraphDefinition
- template_node_id → resolved node fingerprint

它是：

deterministic resolution evidence

不是 durable authority。

不需要 Store。

不需要新的 semantic fingerprint。

真正 semantic authority 仍然是：

resolved Calculation Definition
Calculation Graph fingerprint

============================================================
11. Calculation Registry 必须继续是唯一类型解析 authority
============================================================

Specification 中每个 Calculation Node 必须使用 exact：

kind
type_id
semantic_version

通过现有：

OnlyCalculationTypeReference

禁止：

Python class path
latest semantic version
friendly name guessing
module name
implicit default version

必须通过：

OnlyCalculationRegistry

进行 exact resolution。

必须复用：

resolve_type()
rematerialize_definition()

所有 parameter-derived semantics 必须通过 type-owned resolver 完整重建。

绝不能：

definition.parameters["x"] = ...

然后认为完成。

Calculation Definition 中：

- parameters
- inputs
- outputs
- warmup
- missing-value semantics
- timestamp semantics
- numeric semantics
- factor semantics

必须保持现有 canonical authority。

============================================================
12. P8.0 必须提前验证 RESEARCH backend
============================================================

一个 semantic Calculation Type 存在：

!=

它可以在 Research Runtime 执行。

Specification Resolver 必须验证每个 exact：

kind
type_id
semantic_version

存在：

RESEARCH backend registration

不能：

Specification admitted
        ↓
Research Run queued
        ↓
Worker 执行
        ↓
才发现只有 TRADING backend

可在 compile/admission 时确定的问题必须尽早 fail closed。

注意：

Definition semantic resolution
和
backend availability

是两个不同检查。

保持边界清晰。

============================================================
13. 参数必须 type-preserving
============================================================

复用当前 Calculation scalar codec。

必须保持：

NULL
BOOLEAN
INTEGER
DECIMAL
STRING

例如 Decimal 不得经过 JSON float / Python float / JavaScript number 后再恢复。

逻辑 representation 应保持类似：

{
  "type": "DECIMAL",
  "value": "0.1"
}

必须保证：

"1"
1
Decimal("1")
true

是不同 semantic scalar。

不要创建新的 Specification parameter codec。

============================================================
14. Direct Job 与 Sweep
============================================================

用户层不应该暴露：

direct_jobs
sweeps

这两个已经是 resolved execution composition。

对于用户，本质应该是：

Calculation Spec
+
0 or more Sweep Dimensions

如果：

sweep_dimensions = empty

则 Resolver materialize：

OnlyResearchJobPlan

如果：

sweep_dimensions != empty

则构造/复用：

OnlyResearchSweepDefinition
OnlyResearchSweepPlanner
OnlyResearchSweepPlan

必须继续复用 P7 Sweep semantics：

- finite cartesian expansion
- candidate normalization
- deterministic ordering
- duplicate normalized candidate rejection
- duplicate Calculation identity rejection
- max_cells operational limit

不要重新实现第二套 Sweep compiler。

============================================================
15. Statistics symbolic selector
============================================================

现在 resolved Statistics Plan 需要 exact：

feature:
- calculation_fingerprint
- node_fingerprint
- output_name

target:
- calculation_fingerprint
- node_fingerprint
- output_name

Specification 创建时这些 exact fingerprint 尚不存在。

因此必须建立 symbolic selector，例如：

OnlyResearchSeriesSelector

推荐字段：

- calculation_id
- template_node_id
- output_name

例如：

feature:
  calculation_id = "feature"
  template_node_id = "momentum"
  output_name = "factor_value"

target:
  calculation_id = "target"
  template_node_id = "forward_return"
  output_name = "target_value"

Resolver 必须：

symbolic selector
        ↓
resolved Calculation cell
        ↓
template_node_id → node_fingerprint
        ↓
existing exact Feature/Target Series Reference

最终必须使用现有：

OnlyResearchFeatureSeriesReference
OnlyResearchTargetSeriesReference
OnlyResearchStatisticsPlan

不要创建第二套 resolved statistics reference。

============================================================
16. Statistics semantic validation
============================================================

Resolver 必须提前验证：

Feature selector：
- resolved node kind = FACTOR
- output semantic type ∈ allowed Feature semantics
  如当前正式支持：
  FACTOR_VALUE
  FACTOR_SCORE

Target selector：
- resolved node kind = TARGET
- output semantic type = TARGET_VALUE

必须：
- output 存在
- node 存在
- calculation_id 存在
- template_node_id 存在

Runtime Statistics Executor 继续保留 defensive verification。

这是：

compile-time fail closed
+
runtime defensive validation

而不是两个 authority。

============================================================
17. Statistics Definition
============================================================

直接复用现有：

OnlyResearchStatisticsDefinition

以及已有：
- IC
- RANK_IC
- minimum_observations
- pairing policy
- universe policy
- rank tie semantics
- weighting
- Decimal numeric contract

不要创建：

WebICConfig
SpecificationICConfig
StatisticsV2

Specification Statistics 只负责：

- feature selector
- target selector
- existing statistics definition
- series expansion policy

============================================================
18. Sweep × Statistics expansion
============================================================

这是 P8.0 必须正式冻结的 V1 contract。

假设 Feature selector 对应 N 个 resolved cells，
Target selector 对应 M 个 resolved cells。

P8.0 V1 只允许：

1 × 1
→ 1 Statistics Plan

N × 1
→ N Statistics Plans

1 × N
→ N Statistics Plans

也就是：

BROADCAST_SINGLETON

如果：

N > 1
AND
M > 1

必须：

FAIL CLOSED

不要默认：
- Cartesian
- zip
- assignment join
- parameter-name join

因为这些都是不同的研究语义。

推荐 stable failure：

RESEARCH_SPEC_STATISTICS_EXPANSION_AMBIGUOUS

如果未来真实需求出现，再通过新的 Specification schema version 显式设计：

CARTESIAN
ZIP
JOIN_BY_ASSIGNMENT
...

P8.0 V1 不做。

============================================================
19. Result Plan 自动产生
============================================================

不要让用户 Specification 再手工声明：

result.statistics = [...]

因为用户已经定义了 Statistics Specifications。

Resolver 完成全部 Statistics Plan 后：

all statistics_fingerprints
        ↓
OnlyResearchResultPlan
        ↓
canonical result composition

直接复用现有 Result Plan。

避免重复输入导致：

Specification statistics
!=
Result statistics

这种非法状态。

============================================================
20. Specification fingerprint
============================================================

Specification 必须：
- versioned
- canonical
- serializable
- deterministic
- strict

使用现有 canonical infrastructure：

only_canonical_payload
only_canonical_json
only_canonical_fingerprint

不要创建第二个 canonical JSON 实现。

Specification fingerprint：

SHA256(canonical Specification document)

应包含：
- schema version
- exact Dataset SHA
- symbolic calculation IDs
- graph templates
- exact type references
- typed parameters
- sweep dimensions/candidates
- statistics symbolic selectors
- statistics definitions
- expansion semantics

不包含：

- Runtime ID
- Run ID
- Engine ID
- Worker
- user
- browser
- hostname
- timestamps
- filesystem paths
- object identity
- plugin Python class

特别测试：

Specification-local symbolic ID 改名

可以导致：

specification_fingerprint 改变

但如果 resolved semantics 相同：

graph_fingerprint
calculation/result semantic identity

应保持相同。

这证明：

Specification identity
!=
Research semantic result identity

============================================================
21. Canonical ordering
============================================================

所有 unordered user representation 必须 canonicalize。

至少检查：

calculations
→ stable calculation_id ordering

graph nodes
→ stable template_node_id ordering

bindings
→ stable input ordering

sweep dimensions
→ stable parameter target ordering

sweep candidates
→ semantic type-preserving ordering

statistics specifications
→ deterministic ordering

必须保证：

JSON object key order
dict insertion order
input list order（如果语义上无序）
PYTHONHASHSEED
fresh process

不会改变：

spec fingerprint
resolved graph
resolved workload semantic identities

不要对真正有语义顺序的数据错误排序。

先判断语义，再 canonicalize。

============================================================
22. Resolver
============================================================

推荐建立唯一：

OnlyResearchSpecificationResolver

具体类名按 current truth 调整。

逻辑 pipeline 必须清楚：

Research Specification
        ↓
1. strict schema validation
        ↓
2. canonical normalization
        ↓
3. Dataset SHA validation
        ↓
4. calculation_id uniqueness
        ↓
5. exact type resolution
        ↓
6. RESEARCH backend availability
        ↓
7. Graph Template validation
        ↓
8. Direct materialization / Sweep planning
        ↓
9. exact Job Plans
        ↓
10. symbolic node → exact node index
        ↓
11. resolve Feature/Target selectors
        ↓
12. validate semantic ports
        ↓
13. Statistics expansion
        ↓
14. exact Statistics Plans
        ↓
15. Research Result Plan
        ↓
16. OnlyResearchWorkloadPlan
        ↓
17. existing WorkloadPlan validation
        ↓
RESOLVED

最终执行 contract 必须仍然是：

OnlyResearchWorkloadPlan

禁止创建第二套：

ResolvedExecutionPlanV2

============================================================
23. Resolution Evidence / Candidate Lineage
============================================================

P8.0 必须为未来：

Research
→ select candidate
→ Backtest

保留 deterministic provenance。

推荐 Resolver 返回一个 ephemeral derived result，例如：

OnlyResearchSpecificationResolution

其中至少允许获得：

- specification fingerprint
- workload: OnlyResearchWorkloadPlan
- resolved candidate lineage

Candidate lineage 建议至少可回答：

Specification calculation_id
+
Sweep assignment
        ↓
exact Calculation Graph
        ↓
graph_fingerprint
        ↓
Research Job calculation_fingerprint

以及：

Statistics fingerprint
        ↓
feature candidate
target candidate

注意：

Resolution object：
- 不是新的 semantic authority
- 不持久化
- 没有 Store
- 不成为 Runtime execution input replacement
- 不创造第二个 Result identity

Runtime 仍然执行：

resolution.workload

Future promotion 使用：

exact selected canonical graph identity

============================================================
24. Promotion-ready invariant
============================================================

P8.0 不实现 Backtest promotion。

但必须通过 architecture + tests 冻结：

Research selected candidate
        ↓
exact canonical Calculation Graph

Graph 是未来 promotion boundary。

不是：

Research Calculation Fingerprint

因为 Research Calculation Fingerprint 是 Dataset-bound。

至少新增一个明确回归测试：

Dataset A + Graph G
→ research_calculation_fingerprint A

Dataset B + Graph G
→ research_calculation_fingerprint B

assert A != B

同时：

assert graph_fingerprint_A == graph_fingerprint_B == G

证明：

Dataset-bound Research execution identity
!=
runtime-neutral candidate semantic identity

这个测试非常重要。

============================================================
25. Cross-backend semantic foundation
============================================================

当前 Calculation Registry 已要求：

同一：
kind
type_id
semantic_version

在不同 backend 上具有相同 canonical type definition。

P8.0 不需要实现完整 Research ↔ Trading differential test framework，
但不能破坏这一点。

如果适合当前架构，可增加最小 architecture regression：

- Specification exact type reference 不包含 backend-specific class
- resolved Graph Definition 是 backend-neutral
- Research backend availability 是 execution capability check
- semantic graph identity 不包含 backend implementation identity

不要把：

RESEARCH backend

编码进 Calculation Graph semantic fingerprint。

============================================================
26. Core dependency boundary
============================================================

P8.0 必须保持纯 Core contract。

禁止 Core Specification 依赖：

FastAPI
Pydantic
React
TypeScript
PostgreSQL
SQLAlchemy
Scheduler
Worker
HTTP
Web DTO

推荐继续使用当前风格：

frozen dataclass
Enum
tuple
Mapping
MappingProxyType
to_dict()
from_dict()

未来正确 dependency direction：

Web
 ↓
HTTP DTO
 ↓
Core Research Specification
 ↓
Specification Resolver
 ↓
Existing Research WorkloadPlan
 ↓
Runtime

绝不能反向。

============================================================
27. Schema strictness
============================================================

Specification V1：

schema_version = 1

必须 exact。

以下必须拒绝：

0
2
"1"
true
null

未知字段必须拒绝。

不要：

if unknown_version:
    try latest parser

不要忽略未知字段。

Unsupported / ambiguous：
FAIL CLOSED

============================================================
28. Error model
============================================================

建立稳定的 Specification error boundary。

推荐：

OnlyResearchSpecificationError

包含：
- phase
- code
- detail

phase 可考虑：

SCHEMA
TYPE_RESOLUTION
GRAPH_RESOLUTION
SWEEP_RESOLUTION
SERIES_RESOLUTION
STATISTICS_RESOLUTION
WORKLOAD_VALIDATION

stable code 至少覆盖：

RESEARCH_SPEC_INVALID
RESEARCH_SPEC_SCHEMA_VERSION_UNSUPPORTED
RESEARCH_SPEC_DATASET_INVALID
RESEARCH_SPEC_DUPLICATE_CALCULATION_ID

RESEARCH_SPEC_CALCULATION_TYPE_UNKNOWN
RESEARCH_SPEC_CALCULATION_VERSION_UNKNOWN
RESEARCH_SPEC_RESEARCH_BACKEND_UNAVAILABLE

RESEARCH_SPEC_GRAPH_INVALID
RESEARCH_SPEC_GRAPH_MATERIALIZATION_FAILED

RESEARCH_SPEC_SWEEP_INVALID
RESEARCH_SPEC_SWEEP_CARDINALITY_EXCEEDED

RESEARCH_SPEC_SERIES_REFERENCE_UNKNOWN
RESEARCH_SPEC_FEATURE_SEMANTIC_INVALID
RESEARCH_SPEC_TARGET_SEMANTIC_INVALID

RESEARCH_SPEC_STATISTICS_EXPANSION_AMBIGUOUS
RESEARCH_SPEC_DUPLICATE_STATISTICS

RESEARCH_SPEC_WORKLOAD_INVALID

具体 code 请根据 current error conventions统一。

不要把：

ValueError
KeyError
OnlyResearchSweepError

直接泄漏成未来公共协议。

Resolver 在 domain boundary 做稳定 mapping，
同时保留有价值的 detail / cause。

============================================================
29. 推荐最终代码结构
============================================================

根据 current truth 调整，但推荐职责如下：

src/onlyalpha/research/
│
├── calculation/
│   ├── ...
│   ├── template.py
│   └── template_materialization.py
│
├── specification/
│   ├── __init__.py
│   ├── errors.py
│   ├── model.py
│   ├── identity.py
│   └── resolver.py
│
├── sweep/
├── evaluation/
├── result/
├── artifact/
└── ...

推荐 ownership：

calculation/template.py
-----------------------
OnlyResearchGraphTemplate
OnlyResearchGraphTemplateNode
OnlyResearchTemplateReference
OnlyResearchTemplateInputBinding

calculation/template_materialization.py
---------------------------------------
OnlyResearchGraphTemplateMaterializer
OnlyResearchGraphMaterialization

specification/model.py
----------------------
OnlyResearchSpecification
OnlyResearchCalculationSpec
OnlyResearchSeriesSelector
OnlyResearchStatisticsSpec
Statistics expansion enum

specification/identity.py
-------------------------
RESEARCH_SPECIFICATION_SCHEMA_VERSION
Specification fingerprint

specification/resolver.py
-------------------------
唯一：
Specification → Existing P7 Plans

specification/errors.py
-----------------------
stable P8.0 error boundary

不要为了追求文件数量而机械拆分。
如果 current architecture 指向更干净结构，可以调整，
但必须保持单一职责和单一 authority。

============================================================
30. Public API / imports
============================================================

检查当前：

onlyalpha.research.__init__
onlyalpha.research.sweep.__init__

哪些类已经属于 public API。

如果移动 GraphTemplate：

- 不要复制 implementation
- 必要时通过 re-export 保持兼容
- canonical owner 必须唯一
- 不允许形成 circular dependency

依赖方向应大致变为：

Calculation Registry
        ↑
Research Calculation Template/Materializer
        ↑
        ├── Sweep
        └── Specification Resolver
                  ↓
          Existing Workload Plan

============================================================
31. Acceptance Tests — Serialization / Identity
============================================================

必须覆盖：

1. Specification round trip

spec
→ to_dict
→ JSON-compatible payload
→ from_dict
→ spec

验证：
- canonical document same
- specification fingerprint same
- resolved workload same

2. Unknown fields fail closed

3. Unsupported schema version fail closed

4. typed scalar round trip：
- NULL
- BOOLEAN
- INTEGER
- DECIMAL
- STRING

5. input map ordering does not change fingerprint

6. semantically unordered calculation/statistics declaration order
   不影响 canonical result

7. fresh-process determinism

同一 canonical Specification 在不同 Python process / PYTHONHASHSEED：
- same spec fingerprint
- same graph fingerprints
- same calculation fingerprints
- same statistics fingerprints
- same result plan fingerprint

============================================================
32. Acceptance Tests — Graph / Registry
============================================================

必须覆盖：

- exact type resolution
- unknown type
- unknown semantic version
- RESEARCH backend unavailable
- invalid parameter
- unknown parameter
- missing required parameter
- input dependency invalid
- cycle
- output incompatibility
- Definition resolver rematerializes full semantics
- Direct 和 Sweep 使用同一个 Materializer

必须证明：

P8.0 materialization
没有改变 P7 Calculation identity。

============================================================
33. P7 Identity Regression
============================================================

使用现有 tests 中真实 Research Factor graph / Target graph。

构建：

A. 当前 P7 manual construction
B. P8.0 Specification resolution

要求：

same canonical Calculation Definitions
same node fingerprints
same graph fingerprint
same Research calculation fingerprint
same Statistics fingerprint
same Research Result Plan identity

这是 P8.0 最重要的 regression 之一。

============================================================
34. Acceptance Tests — Sweep
============================================================

至少：

- no sweep → Direct Job
- one dimension
- multiple dimensions
- candidate order independence
- typed Decimal candidate
- normalized duplicate candidate fail
- unknown parameter target
- invalid parameter
- max_cells
- duplicate materialized Calculation identity
- deterministic cell ordering

不要降低现有 P7 Sweep coverage。

============================================================
35. Acceptance Tests — Statistics Expansion
============================================================

明确验证：

1 feature × 1 target
→ 1 Statistics Plan

N feature × 1 target
→ N

1 feature × N target
→ N

N × M where N>1, M>1
→ deterministic FAIL CLOSED

还要覆盖：

- unknown calculation_id
- unknown template_node_id
- wrong output_name
- Feature selector → Indicator
- Feature selector → Target
- Target selector → Factor
- incompatible semantic output
- duplicate Statistics identity

============================================================
36. Acceptance Tests — Candidate Lineage
============================================================

至少证明：

Sweep assignment
→ resolved graph
→ graph fingerprint
→ Research calculation fingerprint
→ Statistics reference

可以 deterministic reverse trace。

例如：

period=20
→ graph G20
→ research calculation R20
→ statistics S20

Future promotion 可以只取：

G20

而不是：

R20

============================================================
37. Acceptance Tests — Dataset-bound vs Runtime-neutral identity
============================================================

必须有明确测试：

Graph G
Dataset A
Dataset B

ResearchJob(A, G).calculation_fingerprint
!=
ResearchJob(B, G).calculation_fingerprint

但：

Graph fingerprint
保持完全相同。

这是 Research → Backtest 单一语义链的基础测试。

============================================================
38. Acceptance Tests — Full Runtime Equivalence
============================================================

使用当前 tests/runtime/research/support.py 中已有 Research product case。

A：
现有 manual OnlyResearchWorkloadPlan

B：
新的 Specification
→ Resolver
→ WorkloadPlan

分别执行完整 finite Research Runtime。

最终必须至少证明：

same calculation fingerprints
same statistics fingerprints
same Research Result fingerprint
same Artifact logical/content identity（按当前 authority contract）
same determinism fingerprint

不要为了通过测试 mock 掉核心 execution。

尽量使用真实现有：
Dataset Store
Calculation Registry
Research Runtime
Result/Artifact Store

============================================================
39. Architecture Tests
============================================================

冻结：

research.specification
X FastAPI

research.specification
X Pydantic

research.specification
X PostgreSQL

research.specification
X SQLAlchemy

research.specification
X Web

research.specification
X Scheduler

research.specification
X Worker

research.specification
X Trading execution authorities

允许依赖：
- Calculation contracts
- Research calculation template/materialization
- Sweep
- Evaluation
- Result Plan
- Research Workload Plan

并确保没有 reverse import。

============================================================
40. Canonical Test Lane
============================================================

如果当前质量体系允许并且符合现有 canonical lane 设计，
为 P8.0 建议新增：

research-specification

canonical test lane。

例如：

uv run python scripts/test_suite.py research-specification

该 lane 应覆盖：

- schema
- identity
- serialization
- graph materialization
- resolution
- sweep composition
- statistics expansion
- candidate lineage
- architecture
- fresh-process determinism
- key runtime equivalence

但：

不要因为本 Prompt 要求就盲目修改质量基础设施。

先读取当前：
scripts/test_suite.py
scripts/verify.py
quality docs

使用 current pattern 正确集成。

============================================================
41. Coverage
============================================================

对于新的纯 specification package：

目标：
100% line
100% branch

如果 shared Materializer 被划入较宽 calculation package，
遵循当前项目 canonical coverage policy。

不要为了 coverage：
- pragma 大量排除
- 删除 meaningful branch
- 写无价值测试

测试行为而不是行数。

============================================================
42. Verification / Quality Gate
============================================================

正常开发过程中使用 targeted tests。

不要每修改一点就执行：

uv run python scripts/test_suite.py core-full --coverage

在实现前：

uv run python scripts/verify.py plan --base "$TASK_BASE_SHA"

确认 impact plan。

实现过程中：
执行最小充分 affected tests。

最终：

uv run python scripts/verify.py agent --base "$TASK_BASE_SHA"

如果本任务修改：
- scripts/test_suite.py
- scripts/verify.py
- CI lane matrix
- coverage infrastructure
- dependency/security verification

则属于 Verification Infrastructure self-change。

按照当前 OnlyAlpha quality system：
在 Task closure 时执行一次要求的 FULL_LOCAL escalation。

只执行 current quality system 要求的重验证。
不要自行创建第四种 Gate。

============================================================
43. Documentation / ADR
============================================================

P8.0 是新的稳定 public input / identity boundary。

必须新增正式 ADR。

编号按当前 ADR 序列确定，不要猜。

ADR 至少冻结：

- Research Specification 的职责
- Specification fingerprint 的含义
- Specification != Result authority
- Dataset exact snapshot reference
- exact Calculation kind/type/version
- no latest / fuzzy lookup
- Graph Template ownership
- unique Template Materializer
- symbolic node → exact node resolution
- RESEARCH backend admission
- typed scalar representation
- Statistics selector semantics
- BROADCAST_SINGLETON V1
- ambiguous N×M fail closed
- Result Plan automatic composition
- Candidate lineage
- Dataset-bound Research calculation identity
- runtime-neutral Graph identity
- promotion-ready candidate semantics
- P8.0 no DB / Run / API / Web ownership

同时新增 P8.0 implementation report，
遵循现有 reports 习惯。

记录：

TASK_BASE_SHA
Goal
Scope
Architecture
Files
Identity invariants
Tests
Coverage
Verification
Known limitations
Out of Scope

README / roadmap 只按照当前工程 current-truth 规则更新。

不要提前宣告：
P8 certified

P8.0 只是 P8 的一个 increment。

============================================================
44. P8.0 明确 Out of Scope
============================================================

绝对不要实现：

PostgreSQL
Database migration
Research Run
Submission Store
Run Store
Scheduler
Worker
Lease
Heartbeat
Attempt
Retry
Cancellation
Command API
new HTTP endpoints
Web form
Run button
Dataset Catalog
ClickHouse
Historical Data Platform
Optimizer
new Statistics algorithms
Backtest promotion endpoint
Strategy Definition
Sim
Live
Trading permission

也不要重构：

Research Result authority
Artifact authority
Query API

除非 current architecture 存在 P8.0 无法绕开的真实 correctness bug；
若发现，先证明必要性，并将修改控制在最小影响范围。

============================================================
45. 非目标：不要过度设计未来 Strategy
============================================================

P8.0 必须为：

Research → Backtest

准备 runtime-neutral candidate semantics。

但不要现在创建：

OnlyStrategyDefinition
Strategy Store
Strategy Promotion API
Backtest Specification

这些属于后续真实阶段。

P8.0 只需要保证：

Research Candidate
        ↓
exact canonical Calculation Graph
        ↓
promotion-ready identity

Target / Statistics / Research Dataset 不进入未来 Strategy semantic identity。

============================================================
46. 最终预期结构
============================================================

P8.0 完成后，系统应形成：

                    Portable User Intent
                            │
                            ▼
                 Research Specification
                            │
                    canonical SHA256
                            │
                            ▼
              Research Specification Resolver
                            │
             ┌──────────────┼──────────────┐
             ▼              ▼              ▼
      Calculation       Graph           Sweep
       Registry       Materializer      Planner
             │              │              │
             └──────────────┼──────────────┘
                            ▼
                  Exact Calculation Graphs
                            │
                  graph fingerprints
                            │
                            ▼
                  Research Job / Sweep
                            │
                            ▼
                Statistics Resolution
                            │
                            ▼
                   Result Plan
                            │
                            ▼
               OnlyResearchWorkloadPlan
                            │
                            ▼
                      OnlyEngine
                            │
                            ▼
                 OnlyResearchRuntime

同时存在 derived provenance：

Specification
    ↓
calculation_id
    ↓
sweep assignment
    ↓
exact Graph G
    ↓
Research calculation R
    ↓
Statistics S

未来：

Research Result
    ↓
select candidate
    ↓
Graph G
    ↓
Future Strategy Composition / Backtest

============================================================
47. P8.0 必须冻结的核心 invariants
============================================================

最终代码、测试和 ADR 必须共同保证：

1.
Specification 是 canonical Research request document，
不是 Research Result authority。

2.
Specification fingerprint 只识别 request document。

3.
Dataset 必须是 exact immutable Snapshot fingerprint。

4.
Calculation 必须 exact：
kind / type_id / semantic_version。

5.
禁止 latest / fuzzy / class-path / hidden fallback。

6.
所有 Calculation Definition 必须通过现有 type-owned resolver 完整 materialize。

7.
RESEARCH backend 必须在 resolution/admission 时可证明存在。

8.
Direct 和 Sweep 共用唯一 Graph Template Materializer。

9.
Statistics symbolic selector 最终解析成现有 exact Series Reference。

10.
V1 Statistics expansion 只允许 singleton broadcast。
多对多 ambiguity fail closed。

11.
Resolver 最终执行 contract 仍然是现有 OnlyResearchWorkloadPlan。

12.
Resolution evidence 不是 durable semantic authority。

13.
Research calculation fingerprint 是 Dataset-bound。

14.
Calculation Graph fingerprint 是 runtime-neutral semantic identity。

15.
Research → Backtest future promotion 必须 preserve exact Graph identity。

16.
Research-specific：
Dataset / Target / Statistics / Result
不得被错误当成 Strategy semantics。

17.
P8.0 不拥有：
DB / Run / Scheduler / API / Web / Trading state。

============================================================
48. 完成标准
============================================================

只有以下全部满足，P8.0 才算实现完成：

A.
可以从 strict versioned serializable Research Specification
deterministically resolve 到现有 OnlyResearchWorkloadPlan。

B.
同一 canonical Specification fresh-process resolution 完全一致。

C.
P8.0 resolved Graph identity 与现有 P7 manual construction 完全一致。

D.
Direct 和 Sweep 不存在重复 materialization algorithm。

E.
Statistics symbolic selector 能正确解析 exact Feature/Target Series。

F.
Sweep Statistics singleton broadcast 正确，
多对多 ambiguity fail closed。

G.
Candidate lineage 可以确定性追踪：
Specification
→ assignment
→ Graph
→ Research Calculation
→ Statistics。

H.
明确证明：
Research calculation identity Dataset-bound，
Graph identity runtime-neutral。

I.
Manual P7 Workload 与 Specification-resolved Workload
执行完整 Research Runtime 后得到同样 semantic/result identities。

J.
没有引入第二套：
Calculation
Statistics
Result
Artifact
execution authority。

K.
没有引入：
PostgreSQL / Scheduler / API / Web 等 P8.1+ 范围。

L.
Architecture tests 和 affected tests 全部通过。

M.
按 scripts/verify.py 的 current impact plan 完成 Task Gate。

N.
ADR + implementation report 完整记录设计与证据。

============================================================
49. 最终输出
============================================================

不要只给设计或 TODO。

完成代码实现、测试和文档。

最终向我报告：

1. TASK_BASE_SHA
2. 实际修改文件
3. 最终架构
4. 新增/移动的 authority
5. Specification V1 contract
6. Resolver pipeline
7. Graph Template / Materializer ownership
8. Candidate lineage 设计
9. Research calculation identity 与 Graph identity 的区别
10. Research → Backtest promotion-ready invariant 如何被代码/测试保证
11. 测试结果
12. coverage 结果
13. scripts/verify.py plan / agent 结果
14. 是否触发 verification-infrastructure FULL_LOCAL escalation
15. ADR / report
16. 剩余已知限制
17. 是否满足 P8.0 完成标准

如果实现过程中发现本 Prompt 的某个具体结构与当前 Repository 冲突：

不要机械执行 Prompt。

必须：
1. 以 Repository current truth 为准；
2. 保住以上 semantic invariants；
3. 采用更简单、更正确、更一致的实现；
4. 在最终报告中说明为什么调整。

最终目标不是“完成 P8.0 功能列表”。

最终目标是建立一个长期稳定的：

Portable Research Intent
→ Deterministic Resolution
→ Existing Exact Research Semantics

边界，

并确保未来：

Research
→ Candidate Selection
→ Backtest
→ Sim
→ Live

不会因为 Runtime 改变而重新解释同一个 Calculation/Strategy semantic definition。