# OnlyAlpha — P7.0.1 Calculation Contract & Verification Closure

Repository:

https://github.com/zongxin1993/OnlyAlpha

你现在负责 OnlyAlpha 的：

> **P7.0.1 — Calculation Contract & Verification Closure**

本阶段是 **P7.0 的工程收口阶段**。

它不是新的 Research Feature 阶段，不实现 Dataset、Vectorized Research Backend、Calculation Store 或 Research Runtime。

本阶段的核心任务是：

> 从第一性原理重新审计 P7.0 已建立的 Calculation Definition / DAG / Registry / Plugin Boundary，关闭其中仍然存在的 contract、identity、version、schema、Factor authority 和 verification 缺口，使 Calculation Foundation 成为可以长期冻结、持久化、扩展并被 P7.1 安全依赖的正式基础设施。

最终目标：

```text
P7.0 Main Implementation
        ↓
P7.0.1 Contract & Verification Closure
        ↓
Calculation Foundation Frozen
        ↓
P7.0 = ACCEPTED
        ↓
P7.1 Readiness = YES
```

如果当前 Repository 的实际证据不满足 Acceptance Gate：

不得为了“完成任务”强行标记为 `ACCEPTED`。

---

# 0. Repository 是唯一 Current Truth

开始任何修改前，必须重新读取当前 Repository。

不得把本 Prompt 中的类名、文件名、历史 SHA、历史判断当成高于 Repository 的事实。

首先执行：

```bash
git status
git branch --show-current
git rev-parse HEAD
git log -10 --oneline
```

记录：

```text
branch
HEAD
working tree status
recent commits
```

历史上曾观察到 P7.0 implementation HEAD：

```text
9204b427366032ebbf99f4a30158874b77da70d4
Feat: P7.0 — Calculation Definition & Plugin Boundary
```

这只是历史参考。

如果 HEAD 已经变化：

> 完全以当前 HEAD、当前 tests、当前 ADR、当前 docs、当前 CI 为事实来源。

不得机械重复实现本 Prompt 中已经被后续提交解决的问题。

---

# 1. 必须重新阅读的工程范围

至少重新检查：

```text
README.md
AGENTS.md
pyproject.toml
uv.lock

docs/roadmap.md
docs/architecture.md
docs/architecture_principles.md
docs/runtime.md
docs/storage.md
docs/engineering/quality-system.md

docs/adr/
尤其 Calculation / Runtime / Plugin / Identity 相关 ADR

src/onlyalpha/canonical.py

src/onlyalpha/calculation/
src/onlyalpha/indicator/
src/onlyalpha/factor/
src/onlyalpha/plugin/
src/onlyalpha/config/
src/onlyalpha/runtime/
src/onlyalpha/cluster/

packages/indicator/
packages/factor/

tests/calculation/
tests/indicator/
tests/architecture/
tests/integration/
tests/scenario/
tests/recovery/

scripts/test_suite.py

.github/workflows/
```

同时搜索：

```text
OnlyCalculationDefinition
OnlyIndicatorDefinition
OnlyFactorDefinition
OnlyCalculationGraphDefinition
OnlyCalculationRegistry
OnlyCalculationBackendRegistration

semantic_version
type_id
schema_version
fingerprint
canonical

OnlyIndicatorTypeId
OnlyIndicatorCreateRequest
OnlyIndicatorSpec
OnlyIndicatorSpecConfig

OnlyFactorFactory
factor_path
config_path

onlyalpha.calculations
```

---

# 2. OnlyAlpha 工程原则

所有工程决策按以下顺序权衡：

> **正确性 > 架构一致性 > 可验证性 > 可恢复性 > 可维护性 > 性能 > 自动化程度**

不得为了：

```text
少写代码
快速完成
保持旧 API 表面不变
减少测试
避免迁移
让 Agent 更容易实现
```

破坏更高优先级目标。

P7.0.1 特别强调：

> **Identity 一旦进入后续 Dataset / Calculation Store / Result / Artifact，就会成为长期持久化 contract。**

因此本阶段宁可 Fail Closed，也不能留下 ambiguous semantic identity。

---

# 3. P7.0.1 的第一性原理问题

P7.0.1 必须重新回答以下问题。

## Question 1

两个 Calculation 到底什么时候是“同一个计算”？

答案不能依赖：

```text
Python object identity
Python class path
process
Runtime
Cluster
filesystem
created_at
random UUID
human alias
```

必须依赖正式 semantic content。

---

## Question 2

一个持久化 Calculation Definition 被未来版本读取时：

如何保证它不会被旧代码错误解释？

答案必须包含：

```text
schema version
exact validation
fail closed
```

---

## Question 3

一个 Calculation Graph 的两个节点连接时：

仅仅因为它们都是 Decimal，就足够证明语义兼容吗？

答案：

```text
NO
```

还需要正式考虑：

```text
data type
nullability
dimensions
semantic type
unit
```

---

## Question 4

Trading 和 Research 的执行模型不同：

是否应该因此建立两套 Calculation Definition？

答案：

```text
NO
```

正确模型是：

```text
ONE Semantic Definition
        │
        ├── Trading Backend
        └── Research Backend
```

---

## Question 5

Registry 是否应该强迫所有 Backend 使用同一种 mutable execution API？

答案：

```text
NO
```

Registry 应统一：

```text
semantic identity
backend availability
exact resolution
```

而不是统一所有执行方法形状。

---

## Question 6

Factor 的 identity 是否应该是：

```text
python.module:ClassName
```

答案：

```text
NO
```

Python class path 是 implementation detail，不是长期 semantic identity。

---

## Question 7

如果配置只写：

```text
EMA
```

系统未来安装：

```text
EMA@1
EMA@2
```

应该选择哪个？

答案不能是：

```text
latest
```

必须得到明确、确定、版本化的 calculation reference。

---

# 4. 本阶段的最终目标结构

P7.0.1 完成后，应形成：

```text
                  ONLYALPHA CORE
                        │
              Calculation Semantics
                        │
       ┌────────────────┼────────────────┐
       │                │                │
 Definition           Graph           Registry
       │                │                │
       │                │          backend-neutral
       │                │                │
       └────────────────┼────────────────┘
                        │
              type_id + semantic_version
                        │
          ┌─────────────┴─────────────┐
          │                           │
      INDICATOR                    FACTOR
      Definition                   Definition
          │                           │
          └─────────────┬─────────────┘
                        │
                  Calculation DAG
                        │
             ┌──────────┴──────────┐
             │                     │
          TRADING                RESEARCH
          Backend                 Backend
          current                 future
             │
        Backtest / SIM
```

关键不变量：

> **Core owns semantics. Plugin owns algorithms. Runtime owns mutable execution state.**

---

# 5. 严格范围控制

P7.0.1 不实现：

```text
Research Runtime
Research Job
Research Plan

Dataset Snapshot
Dataset Store
Dataset Identity implementation

Vectorized Indicator Backend
Vectorized Factor Backend

Calculation Store
Calculation materialization cache

Parameter Sweep

IC
Rank IC
ICIR
Forward Return

Research Result
Research Artifact

Visualization
Query API
Web API
Web UI

ML
LLM Factor Mining
GPU
Distributed Research

LIVE Runtime
P8 Broker Synchronization
P9 Live
```

本阶段也不允许借机重写：

```text
Trading Kernel
Backtest lifecycle
SIM lifecycle
Order semantics
Execution semantics
Position semantics
Account semantics
Transaction semantics
Checkpoint schema
Runtime persistence schema
```

---

# 6. 不得修改 Indicator 数学语义

P7.0.1 是 contract closure。

不是 Indicator Mathematics Rewrite。

当前已有 Indicator，例如：

```text
EMA
SMA
RSI
ATR
Bollinger
Rolling Return
Rolling Volatility
ZScore
MACD
```

原则必须是：

```text
same input
same parameters
same ordering

BEFORE P7.0.1
==
AFTER P7.0.1
```

包括：

```text
output values
ready
warmup
timestamps
duplicate handling
out-of-order handling
reset
checkpoint
post-checkpoint continuation
```

如果发现旧数学 bug：

1. 先记录；
2. 判断是否阻塞当前 contract；
3. 非必要不要在 P7.0.1 修改；
4. 如果确实必须修改计算语义，则必须升级 `semantic_version`；
5. 禁止用相同 semantic version 静默改变算法结果。

---

# 7. Workstream A — Serialization & Schema Closure

重新完整检查：

```text
OnlyCalculationDefinition
OnlyCalculationTypeDefinition
OnlyCalculationReference
OnlyInputDefinition
OnlyOutputDefinition
OnlyWarmupDefinition
OnlyNumericDefinition
OnlyParameterSchema
Calculation Graph serialization
```

确认当前 `from_dict()` / deserialization 是否真正 Fail Closed。

## 必须建立唯一 schema authority

例如：

```python
CALCULATION_DEFINITION_SCHEMA_VERSION = 1
CALCULATION_GRAPH_SCHEMA_VERSION = 1
```

具体命名结合 Repository。

禁止多个模块各自随意定义不同版本。

---

## 7.1 Unsupported Schema 必须拒绝

例如：

```json
{
  "schema_version": 2
}
```

当前程序只支持 v1 时：

```text
FAIL CLOSED
```

禁止：

```text
best effort load
ignore version
fallback to v1
```

---

## 7.2 Unknown Semantic Field 必须拒绝

例如：

```json
{
  "schema_version": 1,
  "...": "...",
  "new_semantic_field": "something important"
}
```

旧 reader 不认识该字段：

```text
FAIL CLOSED
```

原因：

未来新字段可能影响 calculation semantics。

旧代码不能静默忽略。

---

## 7.3 Missing Field 必须拒绝

所有 schema-required semantic field 缺失：

```text
FAIL CLOSED
```

除非该字段在 schema 中正式定义 deterministic default。

---

## 7.4 Nested DTO 同样严格

不能只检查 Calculation Definition 顶层。

至少重新审计：

```text
InputDefinition
OutputDefinition
CalculationReference
WarmupDefinition
NumericDefinition
ParameterDefinition
ParameterSchema
Graph Node
Graph Definition
```

如果它们存在 persistence/public serialization contract：

必须 same discipline。

---

## 7.5 Canonical serialization 必须唯一

继续复用：

```text
onlyalpha.canonical
```

禁止新建另一套 fingerprint philosophy。

必须保证：

```text
dict order
set order
Decimal
enum
None
tuple/list
dataclass
```

处理确定。

---

## 7.6 Serialization Acceptance Tests

至少增加：

```text
round trip
fresh process round trip
unsupported schema
unknown top-level field
unknown nested field
missing required field
wrong enum
wrong scalar type
wrong list/mapping shape
non-canonical invalid reference
```

---

# 8. Workstream B — Complete DAG Semantic Compatibility

重新检查当前 Graph 是否只验证：

```text
data_type
nullability
```

如果是：

必须收口。

Calculation Input / Output contract 中如果已经存在：

```text
data_type
nullable
dimensions
semantic_type
unit
```

则 Graph compatibility authority 必须明确考虑它们。

---

## 8.1 建立唯一 Compatibility Authority

建议优先采用纯函数或小模块，例如：

```text
calculation.compatibility
```

而不是大型 class hierarchy。

目标类似：

```python
only_calculation_output_compatible_with_input(
    output_definition,
    input_definition,
) -> bool
```

或者返回结构化 failure。

不要把逻辑分散在：

```text
Graph
Plugin
Factor
Research
Trading
```

多个位置。

---

## 8.2 Data Type

默认 exact compatibility。

例如：

```text
DECIMAL -> DECIMAL      PASS
STRING  -> STRING       PASS
BOOLEAN -> BOOLEAN      PASS

INTEGER -> DECIMAL      默认 FAIL
```

如果未来需要 conversion：

必须通过显式 Calculation node。

不要隐式 numeric coercion。

---

## 8.3 Nullability

规则：

```text
NON_NULL output
→ NULLABLE input
PASS

NON_NULL output
→ NON_NULL input
PASS

NULLABLE output
→ NULLABLE input
PASS

NULLABLE output
→ NON_NULL input
FAIL
```

除非通过显式 missing-value normalization node。

---

## 8.4 Dimensions

必须定义正式 semantic。

例如：

```text
TIME_SERIES
CROSS_SECTION
TIME × INSTRUMENT
SCALAR
```

或者 Repository 当前的等价表达。

禁止：

```text
TIME SERIES
→ CROSS SECTION
```

因为数据类型都是 Decimal 就通过。

如果未来需要：

```text
cross-sectional rank
aggregation
broadcast
```

这些必须是正式 operator。

---

## 8.5 Semantic Type

例如未来可能存在：

```text
PRICE
RETURN
VOLUME
RATIO
RANK
VOLATILITY
BOOLEAN_SIGNAL
CATEGORY
```

如果当前 schema 已经有 `semantic_type`：

必须真正参与 compatibility。

不要让它变成只写入 fingerprint、但 Graph 不验证的装饰字段。

---

## 8.6 Unit

如果双方声明 unit：

必须明确兼容。

例如：

```text
CNY -> CNY
PASS

USD -> CNY
FAIL

PERCENT -> PRICE
FAIL
```

没有 unit 时如何处理：

必须通过正式规则定义。

不要靠 convention。

---

## 8.7 DAG Test Matrix

至少覆盖：

```text
single node

linear chain
fan-in
fan-out
diamond graph

node input order change
graph insertion order change

duplicate semantic node
missing dependency
cycle

missing output

data type mismatch
nullability mismatch
dimension mismatch
semantic type mismatch
unit mismatch
```

必须证明：

```text
node insertion order
```

不改变：

```text
topological semantic order
graph fingerprint
```

---

# 9. Workstream C — Backend-Neutral Calculation Registry

重新检查当前：

```text
OnlyCalculationRegistry
OnlyCalculationBackendRegistration
OnlyCalculationBackendKind
OnlyTradingBackendFactory
```

如果 Core Registry registration contract 仍被：

```python
create(definition, request)
```

这样的 Trading-specific API 绑死：

需要重构。

---

# 10. Registry 第一性原理职责

Registry 应只负责：

```text
Calculation semantic type registration

identity:
kind
type_id
semantic_version
backend

exact lookup

duplicate detection
backend availability
fail-closed resolution
```

Registry 不应该负责：

```text
Trading lifecycle
batch execution
checkpoint
vectorized compute
Runtime state
```

---

# 11. 推荐 Registry 结构

具体 API 根据当前代码决定，但设计目标类似：

```python
OnlyCalculationBackendRegistration(
    type_definition=...,
    backend=OnlyCalculationBackendKind.TRADING,
    provider=...
)
```

其中：

```text
provider
```

是 backend-specific object。

Calculation Registry 不应强迫：

```text
TRADING provider
RESEARCH provider
```

实现同一种 execution protocol。

---

# 12. Trading Resolver

可以建立明确的 Trading-side resolver，例如：

```text
OnlyTradingCalculationBackendResolver
```

概念流程：

```text
Definition
    ↓
Calculation Registry exact resolve
    ↓
backend = TRADING
    ↓
validate provider implements Trading contract
    ↓
create mutable Trading calculation instance
```

Research 后续可以独立有：

```text
Research Calculation Resolver
```

实现：

```text
compute_batch
```

而不是被迫模拟：

```text
update_bar
snapshot
```

---

# 13. P7.0.1 不注册假的 Research Backend

完成后正确状态依然应该是：

```text
onlyalpha.indicator.ema@1

TRADING:
    AVAILABLE

RESEARCH:
    UNSUPPORTED
```

Registry 应能表达未来 RESEARCH backend。

但当前不得：

```text
placeholder Research backend
Trading backend as Research fallback
fake batch adapter
```

---

# 14. Registry Fail Closed

必须测试：

```text
unknown type_id
unknown semantic_version
unsupported backend
duplicate exact registration
same type/version/backend from two plugins
malformed provider
wrong backend protocol when consumed
```

禁止：

```text
latest semantic version
first registered wins
last registered wins
backend fallback
```

---

# 15. Workstream D — Canonical Calculation Reference

目前重新检查：

```text
OnlyIndicatorTypeId
OnlyIndicatorCreateRequest
OnlyIndicatorSpec
OnlyIndicatorSpecConfig
Trading Config
```

确认产品层是否真正携带：

```text
type_id + semantic_version
```

如果没有：

必须关闭该问题。

---

# 16. Calculation Reference 第一性原理

一个长期可重复的 Calculation reference 至少必须能够明确回答：

```text
这是哪一种 Calculation？
是哪一个 semantic version？
```

例如：

```text
onlyalpha.indicator.ema@1
```

不能只保存：

```text
EMA
```

然后根据当前安装环境决定版本。

---

# 17. 建议新增或明确 Calculation Type Reference

概念上可以是：

```python
OnlyCalculationTypeReference(
    kind=OnlyCalculationKind.INDICATOR,
    type_id="onlyalpha.indicator.ema",
    semantic_version="1",
)
```

实际类名根据 Repository 决定。

要求：

```text
immutable
serializable
canonical
Runtime-independent
backend-independent
```

不要包含：

```text
TRADING
RESEARCH
RuntimeId
ClusterId
Python class path
```

---

# 18. 旧 Indicator Token 的处理

当前如果配置仍使用：

```text
EMA
MACD
RSI
```

不要粗暴破坏现有所有配置。

但必须建立单一 deterministic normalization boundary：

```text
EMA
  ↓
onlyalpha.indicator.ema@1
```

重要：

这个映射必须是明确 compatibility contract。

禁止：

```text
EMA
→ registry.find_latest("ema")
```

---

# 19. Version Omitted 禁止 Dynamic Latest

如果为了当前配置兼容允许：

```text
semantic_version omitted
```

那么 omitted 的含义必须是：

```text
由固定 compatibility mapping 解析到明确版本
```

例如：

```text
EMA legacy token
→ onlyalpha.indicator.ema@1
```

而不是：

```text
→ currently newest installed version
```

---

# 20. Internal Composition 必须使用 Canonical Reference

Normalization boundary 之后：

```text
Cluster
Factor
Indicator Registry
Calculation Registry
checkpoint participant identity
future Research
```

不得继续依赖模糊 legacy token 选择 semantic version。

---

# 21. Workstream E — Factor Canonical Authority Closure

这是本阶段的重点。

重新检查：

```text
OnlyFactorDefinition
OnlyFactorConfig
OnlyFactorFactory
OnlyFactorCreateRequest
OnlyFactorImportConfig

factor_path
config_path

examples/factor/
packages/factor/
```

判断当前 Factor semantic identity 是否仍然主要依赖：

```text
python.module:ClassName
```

如果是：

必须修正。

---

# 22. Factor Semantic Identity

正式 Factor Definition 应可以在：

```text
不启动 Runtime
不创建 Cluster
不 import concrete implementation
```

的情况下完整描述：

> 这个 Factor 是什么。

至少应考虑：

```text
kind = FACTOR

type_id
semantic_version

parameters

inputs
dependencies

Indicator / Factor node references

outputs

dimensions

warmup semantics
missing-value semantics
timestamp semantics
numeric semantics

Factor-specific semantic classification
例如 TIME_SERIES / CROSS_SECTION
```

具体字段以当前 Calculation model 为准。

不要机械重复已经由 Calculation Definition 表达的字段。

---

# 23. Factor Python Class Path 只能是 Implementation Detail

例如：

```text
examples.factor.foo:SomeFactor
```

可以用于：

```text
plugin implementation loading
```

但不能进入：

```text
semantic fingerprint
Factor identity
Calculation DAG semantic node identity
```

---

# 24. Factor Backend Resolution

长期目标：

```text
Factor Definition
      ↓
Calculation Registry
      ↓
TRADING Factor Backend
```

不要长期保持：

```text
Factor Config
      ↓
python path
      ↓
instance
```

作为正式 canonical production path。

---

# 25. Example Factor 不得被错误升级为正式算法

重新判断当前 examples 下的 Factor。

如果只是：

```text
example
test fixture
demo
```

不要为了 P7.0.1 把它们强行升级成：

```text
official onlyalpha.factor.xxx@1
```

正确选择：

### 如果它不是正式产品算法

保留 example 身份。

但清晰标记它不是 canonical built-in Factor authority。

### 如果它已经是正式产品依赖

则必须：

```text
stable type_id
semantic_version
plugin registration
canonical definition
```

不要保持模糊中间状态。

---

# 26. Workstream F — Calculation Plugin Verification Ownership

重新检查：

```text
packages/indicator/onlyalpha-plugin-indicators
packages/factor/onlyalpha-plugin-factors

scripts/test_suite.py
pyproject.toml
quality workflows
coverage config
```

核心原则：

> **Implementation ownership 迁到 Plugin 后，verification ownership 也必须一起迁移。**

不能：

```text
Core 不再拥有算法
但 CI 也不再真正覆盖算法
```

---

# 27. Official Indicator Plugin Tests

为所有正式 built-in Indicator 建立 package-owned characterization tests。

至少覆盖当前真实存在的：

```text
EMA
SMA
RSI
ATR
Bollinger
Rolling Return
Rolling Volatility
ZScore
MACD
```

以 Repository 当前真实算法为准。

---

# 28. 每个 Algorithm 至少冻结以下 Observable Semantics

```text
default parameters

custom parameters

fixed input
fixed expected output

ready
warmup

timestamp

duplicate bar

out-of-order bar

reset

checkpoint capture
checkpoint restore

post-restore continuation

definition semantic identity
```

不要只测试：

```text
isinstance
```

必须有计算结果 characterization。

---

# 29. Golden Test 的原则

Golden / characterization tests 的作用是：

> 证明 P7.0 ownership migration 没有改变已有 observable semantics。

不要用新测试重新发明另一套指标定义。

Expected values 应来自：

```text
现有正式算法语义
迁移前 characterization
明确数学 contract
```

---

# 30. Property / Metamorphic Testing

当前工程如果已经使用 Hypothesis：

优先增加 property tests。

至少测试：

## Same semantics -> same fingerprint

```text
dict insertion order changes
explicit default vs implicit default
fresh process
serialization round-trip
human alias change
graph insertion order
```

应：

```text
same fingerprint
```

---

## Semantic change -> fingerprint changes

改变：

```text
parameter
input binding
semantic version
output contract
warmup
missing policy
timestamp semantic
numeric semantic
dimensions
semantic type
unit
Factor semantics
```

应：

```text
fingerprint changes
```

---

## Presentation change

例如：

```text
alias
display label
```

如果正式定义为 presentation-only：

应：

```text
fingerprint unchanged
```

---

# 31. Plugin Discovery Contract

继续复用当前：

```text
onlyalpha.calculations
```

entry point。

不要造第二套 discovery system。

补充测试：

```text
stable ordering
empty provider
valid provider
malformed provider
provider raises
duplicate registrations across plugins
unsupported registration
wrong registration shape
```

注意：

如果官方 Factor plugin 当前没有 concrete Factor：

```text
empty tuple
```

应可以是合法结果。

不要为了测试方便创造假 Factor。

---

# 32. Workstream G — Architecture Enforcement

保留现有：

```text
Core cannot import concrete calculation plugin

Calculation semantic layer cannot depend on:
Runtime
Cluster
Account
Broker
Order
Position
Transaction
```

并重新评估是否需要补充：

```text
Calculation Definition cannot import mutable Indicator runtime APIs

Calculation Definition cannot import Factor lifecycle APIs

Calculation Registry cannot import concrete plugins

Calculation semantic fingerprint cannot depend on:
RuntimeId
ClusterId
filesystem path
class path
created_at
random UUID

plugins cannot create second canonical fingerprint authority

future Research packages cannot import Trading Kernel authority
```

优先使用：

```text
import-linter
AST architecture tests
```

而不是只写 documentation convention。

---

# 33. 禁止第二套 Authority

本阶段绝对禁止出现：

```text
CalculationDefinitionFingerprint
和
IndicatorDefinitionFingerprint
```

两套不同权威算法。

或者：

```text
CalculationRegistry
和
IndicatorRegistry
```

各自独立决定 semantic version。

需要明确：

```text
Calculation Definition
Calculation Graph
Calculation Registry
```

是 semantic authority。

Trading Indicator Registry 可以继续负责：

```text
mutable instance scope
Factor ownership
runtime instance lookup
```

但不能重新成为 semantic identity authority。

---

# 34. Workstream H — Quality Toolchain Closure

重新检查：

```text
scripts/test_suite.py
pyproject.toml
.github/workflows/quality.yml
.github/workflows/codeql.yml
```

确保新 Calculation plugins 正式进入质量系统。

---

# 35. Mypy

当前 official plugin package 必须被 type checking。

建议 CI 显式执行：

```text
mypy core
mypy indicator plugin
mypy factor plugin
```

如果 root mypy config 已经涵盖它们：

也应确保 CI 实际 command 使用该 config。

不要出现：

```text
pyproject 声明检查 plugin
但 CI command 只检查 src/onlyalpha
```

导致真实 coverage 不明确。

---

# 36. Canonical Test Lane

建议根据当前 test suite 结构增加：

```text
calculation
```

lane。

建议覆盖：

```text
Calculation Definition contract
serialization
fingerprint
DAG
registry
plugin discovery
Indicator characterization
semantic equivalence
Factor canonical contract
```

如果当前 test suite 不适合新增 lane：

也可以纳入 `core-full`。

关键不是 lane 名字。

关键是：

> Official Calculation Foundation 必须有一个明确、稳定、可重复的 canonical verification path。

---

# 37. Coverage Ownership

重新检查当前 coverage 是否只统计：

```text
src/onlyalpha
```

如果 concrete Indicator algorithms 已迁入 packages：

必须明确 coverage policy。

推荐：

## Core

保持：

```text
Core branch coverage >= current project threshold
```

## Official Indicator Plugin

建立独立 plugin coverage gate。

阈值应根据当前真实 baseline 合理制定。

不要为了通过而降低已有质量标准。

不要机械要求一个没有证据支持的超高阈值。

核心目标：

> Plugin implementation 不能因为离开 Core 而逃出 coverage authority。

---

# 38. Build

必须保证：

```text
uv build --all-packages
```

真实覆盖：

```text
Core
Indicator plugin
Factor plugin
所有现有 workspace package
```

---

# 39. Trading Regression

P7.0.1 完成后必须证明没有破坏 P6 Trading baseline。

至少运行当前 canonical：

```text
core-full
recovery
sim-recovery
ashare
miniqmt-contract
```

以及当前 Repository 定义的其它 mandatory lane。

重点证明：

```text
Backtest unchanged
SIM unchanged
checkpoint unchanged
restart unchanged
Factor / Indicator Trading composition unchanged
```

---

# 40. Workstream I — Documentation / Repository Truth

完成代码与测试后：

重新对齐：

```text
README.md
docs/roadmap.md
docs/architecture.md
docs/architecture_principles.md
docs/engineering/quality-system.md

ADR 0069
或新增最合适 ADR
```

不要覆盖历史 ADR 原因。

如果 P7.0.1 实质改变了 Registry / semantic reference / Factor authority：

应新增 ADR，或根据 ADR policy 做正确处理。

---

# 41. 文档必须区分

```text
Target Architecture
Current Implementation
Accepted Contract
Known Limitations
Future Work
```

不要把：

```text
future Research backend
```

写成已实现。

---

# 42. P6 状态必须保持真实

如果当前 README / Roadmap 仍然把已经 certified 的 P6 写成：

```text
remote certification pending
```

应根据 Repository 当前真实 certification evidence 修正。

但不要重新定义 P6 subject。

历史认证 subject 应保持 immutable。

---

# 43. P7.0 状态

只有在 P7.0.1 验收完成后：

才允许写：

```text
P7.0 = ACCEPTED
```

否则只能：

```text
CONDITIONALLY_ACCEPTED
```

或：

```text
REJECTED
```

---

# 44. P7.1 状态

本任务只允许把 P7.1 标记：

```text
OPEN / READY
```

不得开始实现。

建议 P7.1 后续方向：

> **Research Dataset Snapshot & Deterministic Dataset Identity**

但本任务不要写 Dataset implementation。

---

# 45. Refactoring Discipline

所有修改遵循：

```text
Characterization
    ↓
Contract
    ↓
Small Refactor
    ↓
Targeted Tests
    ↓
Full Regression
```

不要一次同时进行：

```text
Calculation semantic refactor
Indicator math rewrite
Factor API redesign
Config rewrite
Research implementation
```

---

# 46. 尽量小步改变

优先：

```text
extract authority
move validation
replace ambiguous identity
introduce exact reference
adapt old composition
add tests
```

而不是：

```text
rewrite entire indicator/factor subsystem
```

---

# 47. Config Compatibility 原则

如果 current product config 有大量：

```text
indicator_type: EMA
factor_path: ...
```

不要为了架构纯洁直接让所有历史测试/配置全部失效。

但 compatibility 必须满足：

```text
one explicit normalization boundary
deterministic mapping
no latest resolution
no duplicate authority
```

不要让 legacy token 继续渗透内部 semantic identity。

---

# 48. Persistence / Checkpoint Compatibility

默认不修改：

```text
Runtime checkpoint schema
Indicator checkpoint payload
Persistence schema
Transaction schema
Runtime participant identity
```

如果 canonical semantic reference 必须影响新 configuration identity：

先检查它是否会改变已有 restart/checkpoint compatibility。

任何变化必须：

```text
明确
测试
fail closed
```

不得 silent migration。

---

# 49. No False Green

严禁：

```text
删除失败测试
skip 新失败测试
扩大 skip
降低 coverage threshold
弱化 architecture test
弱化 Semgrep
吞异常
silent fallback
latest fallback
自动修正非法 schema
自动选择 backend
自动选择 version
增加 sleep
把 failure 变 warning
```

如果正确性要求测试失败：

保持失败，报告 blocker。

---

# 50. 建议实施顺序

## Phase A — Current Truth & Characterization

1. 重新读 Repository。
2. 输出当前 architecture map。
3. 验证本 Prompt 中每个 suspected gap 是否仍存在。
4. 对将被修改的 observable behavior 增加 characterization tests。

---

## Phase B — Serialization Closure

完成：

```text
schema authority
exact field validation
fail-closed deserialize
nested DTO closure
```

运行 targeted calculation tests。

---

## Phase C — DAG Compatibility Closure

完成：

```text
type
nullability
dimensions
semantic_type
unit
```

唯一 compatibility authority。

运行完整 DAG matrix。

---

## Phase D — Registry Closure

完成：

```text
backend-neutral Registry
Trading-specific resolver
exact provider validation
```

不实现 Research backend。

---

## Phase E — Semantic Reference & Factor Closure

完成：

```text
type_id + semantic_version product reference
legacy deterministic normalization
Factor semantic authority
class path demotion to implementation detail
```

这是风险最高阶段。

必须小步完成。

---

## Phase F — Plugin Verification

增加：

```text
package tests
golden tests
checkpoint continuation
property tests
plugin discovery tests
mypy
coverage
calculation lane
```

---

## Phase G — Full Validation

完整跑：

```text
static
build
calculation
core-full
recovery
sim-recovery
ashare
miniqmt-contract
coverage
security
```

---

## Phase H — Independent Review

重新审查：

```text
duplicate authority
identity ambiguity
schema compatibility
Graph compatibility
Trading regression
future Research extensibility
```

---

## Phase I — Documentation Reconciliation

最终再更新 docs。

不要先把阶段写成 ACCEPTED 再测试。

---

# 51. Required Tests

至少应存在以下类别。

## Serialization

```text
round trip
fresh process
schema mismatch
unknown fields
missing fields
wrong types
```

## Fingerprint

```text
same semantic definition -> same fingerprint
semantic mutation -> different fingerprint
presentation mutation -> same fingerprint
```

## DAG

```text
linear
fan-in
fan-out
diamond
cycle
missing dependency
missing output
type mismatch
nullable mismatch
dimension mismatch
semantic type mismatch
unit mismatch
```

## Registry

```text
exact resolve
unknown type
unknown version
unknown backend
duplicate registration
provider protocol mismatch
cross-plugin collision
```

## Version Resolution

```text
legacy token -> deterministic canonical reference
explicit version -> exact version
no latest fallback
two versions coexist
```

## Factor

```text
Factor Definition serializable
Factor fingerprint stable
class path excluded
Runtime/Cluster excluded
dependency identity deterministic
```

## Indicator Characterization

所有 official algorithm：

```text
fixed output
ready
warmup
timestamp
reset
checkpoint
continuation
```

## Architecture

```text
core -> plugin forbidden
calculation semantics -> runtime forbidden
class path -> semantic fingerprint forbidden
duplicate fingerprint authority forbidden
```

---

# 52. Validation Commands

基于当前 Repository 的真实工具链执行。

至少：

```bash
uv sync --frozen --all-packages --all-groups
```

Static：

```bash
uv run ruff check src tests examples packages scripts
uv run ruff format --check src tests examples packages scripts
uv run mypy src/onlyalpha
uv run lint-imports
uv run python scripts/version_sync.py check
```

同时运行所有 official workspace package 的 mypy。

如果 P7.0.1 建立 `calculation` lane：

```bash
uv run python scripts/test_suite.py calculation
```

然后：

```bash
uv run python scripts/test_suite.py core-full
uv run python scripts/test_suite.py recovery
uv run python scripts/test_suite.py sim-recovery
uv run python scripts/test_suite.py ashare
uv run python scripts/test_suite.py miniqmt-contract
```

Coverage：

```bash
uv run python scripts/test_suite.py core-full --coverage
```

以及新增 official calculation plugin coverage gate。

Build：

```bash
uv build --all-packages
```

如果当前 release lane 是 canonical：

```bash
uv run python scripts/test_suite.py release
```

不要使用本 Prompt 中已经失效的命令。

以当前 scripts 为准。

---

# 53. Remote Evidence

GitHub Actions：

```text
Layered Quality
CodeQL
Semgrep
```

如果本地不能运行：

必须写：

```text
REMOTE REQUIRED
```

不能伪造 PASS。

如果当前阶段有正式 P7 certification workflow：

按 Repository 当前 policy 执行。

如果没有：

不要为了 P7.0.1 临时复制 P6 certification workflow，除非 Quality System 明确要求。

---

# 54. P7.0.1 Acceptance Gate

只有以下全部满足，P7.0 才能最终升级为 ACCEPTED。

## Calculation Definition

- [ ] immutable
- [ ] Runtime-independent
- [ ] deterministic serialization
- [ ] stable fingerprint
- [ ] schema version enforced
- [ ] unknown semantic field rejected
- [ ] unsupported schema rejected

## Semantic Identity

- [ ] stable `type_id`
- [ ] explicit `semantic_version`
- [ ] no dynamic latest
- [ ] Python class path not semantic identity
- [ ] Runtime / Cluster identity excluded
- [ ] presentation alias excluded where appropriate

## DAG

- [ ] acyclic
- [ ] deterministic
- [ ] missing dependency rejected
- [ ] output reference validated
- [ ] data type validated
- [ ] nullability validated
- [ ] dimensions validated
- [ ] semantic type validated
- [ ] unit validated

## Registry

- [ ] backend-neutral
- [ ] exact resolve
- [ ] duplicate rejected
- [ ] backend fallback forbidden
- [ ] semantic-version fallback forbidden
- [ ] only real backend advertised as available

## Indicator

- [ ] all official algorithms owned by plugin
- [ ] no duplicate Core implementation
- [ ] characterization complete
- [ ] checkpoint continuation preserved

## Factor

- [ ] canonical Factor semantic definition exists
- [ ] Factor semantic identity independent of Python path
- [ ] no fake built-in Factor created
- [ ] current Trading integration has a clear canonical path

## Quality

- [ ] Core mypy PASS
- [ ] Indicator plugin mypy PASS
- [ ] Factor plugin mypy PASS
- [ ] import-linter PASS
- [ ] calculation tests PASS
- [ ] core-full PASS
- [ ] recovery PASS
- [ ] sim-recovery PASS
- [ ] ashare PASS
- [ ] miniqmt-contract PASS
- [ ] coverage PASS
- [ ] build PASS
- [ ] Semgrep PASS where required
- [ ] CodeQL PASS where required

## Documentation

- [ ] README accurate
- [ ] roadmap accurate
- [ ] ADR accurate
- [ ] quality-system accurate
- [ ] P6 state accurate
- [ ] P7.0 state evidence-backed
- [ ] P7.1 scope clear but not implemented

## Independent Review

- [ ] Critical = 0
- [ ] High = 0

---

# 55. Final Decision

最终只允许：

```text
ACCEPTED
CONDITIONALLY_ACCEPTED
REJECTED
```

## ACCEPTED

表示：

```text
所有 mandatory P7.0.1 contract 已闭环
所有 mandatory verification 有真实证据
无 Critical/High
```

此时：

```text
P7.0 = ACCEPTED
P7.1 Readiness = YES
```

---

## CONDITIONALLY_ACCEPTED

表示核心实现正确，但仍存在明确且有限的外部条件，例如：

```text
remote CI evidence pending
```

如果仍存在 Calculation identity / schema / Factor authority High 缺口：

不能用 CONDITIONALLY_ACCEPTED 掩盖。

---

## REJECTED

表示存在：

```text
semantic identity ambiguity
schema safety failure
duplicate authority
Trading regression
broken plugin boundary
mandatory quality failure
```

---

# 56. Final Report Format

完成后必须输出：

# P7.0.1 Calculation Contract & Verification Closure Report

## 1. Repository State

```text
branch
initial HEAD
final HEAD / working tree
working tree status
```

---

## 2. Current Truth Reassessment

逐项说明本 Prompt 识别的问题：

```text
Serialization
DAG compatibility
Registry
semantic version
Factor authority
Plugin verification
Documentation
```

每项标记：

```text
CONFIRMED
ALREADY RESOLVED
NOT APPLICABLE
NEW FINDING
```

不要机械假设问题仍存在。

---

## 3. Root Causes

不要只说：

```text
缺少字段
缺少测试
```

要说明为什么会产生架构风险。

例如：

```text
Product configuration cannot pin semantic version,
therefore installed backend set can affect calculation meaning.
```

---

## 4. Architecture Decisions

说明最终决定：

```text
schema authority
DAG compatibility authority
backend-neutral Registry boundary
Trading resolver boundary
Calculation type reference
legacy normalization policy
Factor semantic identity
plugin verification ownership
```

---

## 5. Changes Made

逐文件列出：

```text
path
change
reason
protected invariant
```

---

## 6. Semantic Compatibility

明确说明哪些行为没有变化：

```text
Indicator math
ready
warmup
timestamp
duplicate handling
out-of-order handling
checkpoint payload
checkpoint continuation
Backtest
SIM
```

只列真实确认的项目。

---

## 7. Tests Added / Changed

格式：

```text
test
→ contract proved
```

不要写：

```text
added tests
```

---

## 8. Local Validation

给出真实结果。

例如：

```text
ruff: PASS
mypy core: PASS
mypy indicators: PASS
mypy factors: PASS
lint-imports: PASS

calculation: X passed
core-full: X passed
recovery: X passed
sim-recovery: X passed
ashare: X passed
miniqmt-contract: X passed

core branch coverage: X%
indicator plugin branch coverage: X%

build: PASS
```

不得猜测。

---

## 9. Remote Validation

列出：

```text
workflow
subject SHA
actual conclusion
```

无法验证则：

```text
REMOTE REQUIRED
```

---

## 10. Remaining Known Limitations

只列真实 future scope，例如：

```text
RESEARCH backend not implemented
Dataset Snapshot not implemented
Calculation Store not implemented
Research Runtime not implemented
```

不要把这些正常 P7.1+ scope 写成 P7.0.1 bug。

---

## 11. P7.0 Final Decision

只能：

```text
ACCEPTED
CONDITIONALLY_ACCEPTED
REJECTED
```

说明依据。

---

## 12. P7.1 Readiness

明确：

```text
P7.1 Readiness = YES
```

或：

```text
P7.1 Readiness = NO
```

---

## 13. Recommended Next Step

如果：

```text
P7.0 = ACCEPTED
```

下一步建议：

> **P7.1 — Research Dataset Snapshot & Deterministic Dataset Identity**

不要在当前任务继续实现。

---

# 57. P7.1 未来边界预留

仅为了保证当前 contract 不堵死未来设计，P7.0.1 应允许未来：

```text
Historical Cache / Provider
        ↓
Dataset Definition
        ↓
Dataset Snapshot
        ↓
Dataset Fingerprint
        ↓
Calculation Definition
        ↓
RESEARCH Backend
        ↓
Calculation Result
```

但 P7.0.1 不实现任何上述新 Research capability。

---

# 58. 最终必须回答的六个问题

完成后必须明确回答：

## Q1

不启动 Runtime：

能不能完整描述一个 Indicator？

必须：

```text
YES
```

## Q2

不启动 Runtime：

能不能完整描述一个正式 Factor？

必须：

```text
YES
```

## Q3

同一个 Calculation Definition：

不同进程、不同 dict order、不同 presentation alias 是否得到同一 semantic fingerprint？

必须：

```text
YES
```

## Q4

未知 future schema：

旧程序会不会静默加载？

必须：

```text
NO
```

## Q5

未来真实 Research Backend：

能不能注册同一个 semantic definition 而无需重写 Calculation Registry 核心 contract？

必须：

```text
YES
```

## Q6

安装 EMA@2 后：

已有 EMA@1 Calculation reference 是否仍然确定地指向 EMA@1？

必须：

```text
YES
```

任何一个答案不是预期结果：

> P7.0 尚未完成。

---

# 59. Coding Style

要求：

```text
模块职责单一
依赖方向明确
类型清晰
immutable model 优先
pure function 优先
Fail Closed
small public API
无重复 authority
无 magic fallback
无 giant registry
无 service locator 滥用
无 arbitrary dict 逃逸正式 schema
无 unnecessary abstraction
```

不要为了“架构感”增加：

```text
BaseManager
AbstractResolverManager
GenericCalculationServiceFactoryProvider
```

这种无实际约束价值的层级。

抽象必须解决真实变化轴。

---

# 60. 模块边界原则

推荐思考：

```text
calculation.definition
    什么是 Calculation

calculation.graph
    Calculation 如何形成 semantic DAG

calculation.compatibility
    两个 semantic ports 是否可以连接

calculation.registry
    哪个 backend 实现某个 exact semantic type/version

indicator
    Trading mutable Indicator-facing contract / scope

factor
    Trading mutable Factor-facing contract / scope

plugins
    concrete algorithms
```

具体目录以当前 Repository 为准。

不要为了匹配以上示意强行移动所有文件。

---

# 61. 最重要的禁止事项

以下任一结果都视为架构失败：

```text
1. Registry 仍然只能表达 Trading factory。

2. 为了未来 Research 写假的 RESEARCH backend。

3. semantic_version 只存在底层，但产品 config 无法确定引用版本。

4. 找不到 version 时自动选择 latest。

5. Factor identity 仍然等于 Python class path。

6. Python class path 进入 Calculation fingerprint。

7. RuntimeId / ClusterId 进入 Calculation fingerprint。

8. unknown serialization field 被静默忽略。

9. future schema 被旧代码 best-effort 加载。

10. Graph 只检查 Decimal 而不检查 semantic compatibility。

11. Plugin 搬走以后不再有正式测试/coverage。

12. 创建第二套 canonical hash/fingerprint authority。

13. P7.0.1 顺手实现 Dataset / Research Backend。

14. 在相同 semantic version 下偷偷修改 Indicator 数学。

15. 为了让测试绿降低质量门槛。
```

---

# 62. 最终目标

P7.0.1 的真正产物不是：

```text
更多 validation
更多 tests
更多 classes
```

而是一个可以冻结的 Calculation Contract：

```text
Stable Semantic Definition
        +
Explicit Version
        +
Fail-Closed Schema
        +
Semantically Valid DAG
        +
Backend-Neutral Registry
        +
Canonical Factor Authority
        +
Verified Plugin Implementations
        =
Frozen Calculation Foundation
```

这个 Foundation 一旦完成：

后续：

```text
Dataset Snapshot
Calculation Store
Vectorized Research
Research Result
Artifact
Web
ML / LLM Factor Mining
```

都可以引用同一套 Calculation identity，而不需要再次迁移 foundational semantics。

这才是 P7.0.1 的成功标准。

---

# 63. Execution Rule

现在开始执行。

顺序必须是：

```text
1. Re-read current HEAD
2. Reassess every suspected issue
3. Build characterization evidence
4. Fix schema safety
5. Fix DAG semantic compatibility
6. Make Registry backend-neutral
7. Close semantic version reference
8. Close Factor canonical authority
9. Close plugin verification ownership
10. Run targeted tests
11. Run full Trading regression
12. Run complete quality gates
13. Independent architecture review
14. Reconcile documentation
15. Decide ACCEPTED / CONDITIONALLY_ACCEPTED / REJECTED
```

不要预设：

```text
P7.0 = ACCEPTED
```

最终状态必须由代码、测试和真实 evidence 决定。

Repository HEAD 永远高于本 Prompt 中的历史假设。