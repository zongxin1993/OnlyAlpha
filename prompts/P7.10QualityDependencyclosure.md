你正在维护 OnlyAlpha：

Repository:
https://github.com/zongxin1993/OnlyAlpha

本任务是一次“当前主线工程质量与文档收敛”，不是新的 P7 功能开发。

============================================================
0. 核心原则
============================================================

OnlyAlpha 的工程决策优先级：

正确性
> 架构一致性
> 可验证性
> 可恢复性
> 可维护性
> 性能
> 自动化程度

必须遵守：

1. Repository 当前 HEAD 是唯一 current truth。
2. 开始工作前必须重新阅读当前仓库，不得机械依赖本 Prompt 中记录的旧 SHA、旧状态或历史结论。
3. 当前代码、测试、配置、CI workflow、ADR、README、工程文档优先于历史讨论。
4. 不允许为了让 CI 变绿而：
   - 跳过测试；
   - 关闭安全扫描；
   - 增加漏洞白名单而没有工程依据；
   - 降低 coverage；
   - 放宽 architecture guard；
   - 删除失败测试；
   - 使用 continue-on-error 掩盖真实失败；
   - 修改 fail-closed 语义为 fail-open；
   - 伪造 verification / certification evidence。
5. 本任务不得顺带实现新的 Research / Web / Runtime / Trading 功能。
6. 不进行无关重构。
7. 不把质量收敛任务扩展为“大规模依赖升级”。
8. 每项修改都必须能解释：
   - 为什么需要改；
   - 哪个 contract 受影响；
   - 哪些测试能够证明修改正确。

============================================================
1. 任务目标
============================================================

将当前 OnlyAlpha 主线收敛到：

- dependency contract 自洽；
- authoritative uv.lock 不包含当前质量门禁明确拒绝的已知依赖漏洞；
- pytest/toolchain version contract 与 workspace package contract 一致；
- pytest major-version 变化不会破坏测试体系；
- static / architecture / build / affected tests 正常；
- dependency-audit 恢复绿色；
- README / roadmap / validation docs / certification state 与当前仓库真实状态一致；
- 不改变任何业务语义；
- 不引入新的产品能力；
- 当前 HEAD 可以作为下一 P7 increment 的 clean baseline。

这是一项：

Quality + Dependency + Documentation Closure

而不是：

Feature Development

============================================================
2. 开始前必须重新调查
============================================================

不要立即修改代码。

首先重新读取并理解至少以下内容。

根配置：

- pyproject.toml
- uv.lock
- .python-version（如果存在）
- workspace/package 配置
- scripts/test_suite.py

质量基础设施：

- .github/workflows/quality.yml
- .github/workflows/codeql.yml
- P7 / Final-SHA certification workflow
- dependency audit / OSV scanner 相关脚本和配置
- coverage 配置
- ruff 配置
- mypy 配置
- import-linter / architecture guard
- Semgrep guardrail（如果存在）

API package：

- packages/api/onlyalpha-api/pyproject.toml
- packages/api/onlyalpha-api/src/**
- packages/api/onlyalpha-api/tests/**

工程状态文档：

- README.md
- docs/roadmap.md 或当前 roadmap authority
- docs/engineering/**
- docs/validation/**
- docs/adr/**
- 当前 P7.10 validation / verification 文档
- P6 Final-SHA certification 相关记录

同时检查：

- 当前 HEAD SHA；
- 最近相关 commits；
- 当前 GitHub Actions 状态；
- 当前 dependency-audit failure 的真实原因。

如果当前仓库已经修复了本 Prompt 描述的某个问题，
以当前 HEAD 为准，不要重复修改。

============================================================
3. 当前已知问题——必须重新验证，不得直接假定
============================================================

此前主线 P7.10 HEAD 曾出现以下问题：

dependency-audit 在 authoritative uv.lock 上失败。

已观察到：

pytest 8.4.2

命中：

CVE-2025-71176
GHSA-6w46-j5rx-g56g

安全修复版本为：

pytest >= 9.0.3

与此同时：

packages/api/onlyalpha-api/pyproject.toml

曾存在类似：

pytest>=8.4,<9

的依赖约束。

因此形成：

OSV security requirement:
    pytest >= 9.0.3

versus

onlyalpha-api package contract:
    pytest < 9

的冲突。

必须首先重新确认：

- 当前 uv.lock 是否仍然锁定漏洞版本；
- 当前 API package 是否仍然限制 pytest < 9；
- root pyproject 是否存在其它 pytest version contract；
- workspace 是否存在多个相互冲突的 pytest declarations；
- pytest 9 是否对 OnlyAlpha 当前 tests/plugins/configuration 存在兼容性变化。

不要只运行：

uv lock --upgrade-package pytest

然后结束。

如果根因仍然存在，必须同时解决：

Dependency declaration
+
Workspace version contract
+
Lockfile
+
pytest 9 compatibility
+
quality verification

============================================================
4. Dependency / pytest 收敛要求
============================================================

如果当前问题仍然存在：

### 4.1 统一 pytest contract

找到所有正式 pytest version declarations。

避免不同 package 各自定义互相冲突的 pytest contract。

目标：

- authoritative version requirement 清晰；
- workspace package 不阻止安全版本；
- 不无理由锁死具体 patch；
- 不引入不必要的大范围 toolchain upgrade。

如果 pytest >= 9.0.3 是当前安全修复所必需，
应允许使用安全版本。

但必须先检查 pytest 9 compatibility。

### 4.2 更新 lockfile

使用 uv 正式机制重新解析 lock。

不得手工编辑 uv.lock。

确保：

uv sync --frozen --all-packages --all-groups

在新的 lockfile 下能够成功。

### 4.3 验证 pytest 9 compatibility

重点检查：

- pytest config；
- pytest plugins；
- fixtures；
- monkeypatch；
- tmp_path；
- markers；
- warnings；
- collection；
- async tests（如果有）；
- Hypothesis integration；
- coverage integration；
- package-local pytest configuration；
- test discovery；
- conftest.py；
- 自定义 pytest hooks。

如果出现 compatibility regression：

修复真正的不兼容代码或配置。

不得：

- pin 回漏洞版本；
- 禁用失败测试；
- 降级安全门禁。

============================================================
5. Dependency audit 收敛
============================================================

重新理解 OnlyAlpha 当前 dependency audit contract。

当前设计原则应继续保持：

authoritative uv.lock
        ↓
pinned OSV Scanner
        ↓
fail-closed scan
        ↓
machine-readable evidence
        ↓
quality gate

必须确保：

- scanner 仍然 fail closed；
- scan subject 仍然是 authoritative lockfile；
- evidence generation 仍然真实反映扫描结果；
- GitHub Actions 不因本次修复改变安全含义；
- 不添加无依据 ignore；
- 不通过 wrapper 吞掉 scanner exit code。

如果 dependency-audit 失败不是 pytest，而是当前 HEAD 出现了新的 finding：

必须调查真实 finding。

不要只针对历史 pytest 问题写死解决方案。

============================================================
6. 文档 current-truth 收敛
============================================================

系统检查工程状态类文档。

重点检查 README 中类似：

- Current version
- Product stage
- Current increment
- P6 status
- P7 status
- certification status
- Next semantic direction
- Research Runtime status
- P8 entry gate

等信息。

此前曾观察到 README 声明：

P6:
Implemented and locally verified;
final-SHA remote certification still required

但 GitHub Actions 中已经存在成功的：

P6 Final-SHA Certification

所以如果当前 README 仍然保留这种陈旧描述：

必须修正。

但是不要仅修改这一行。

需要搜索整个仓库是否还有：

- “P6 certification pending”
- “P6 remote certification required”
- 已失效的 SHA；
- 已过时的 P7 increment 状态；
- 已完成却仍写 pending 的 validation 项；
- 已改变却仍描述旧 architecture 的文档；
- 把 development verification 写成 certification 的内容；
- 把 same-milestone VERIFIED 错写为 milestone CERTIFIED 的内容。

必须严格保持以下语义区别：

VERIFIED
!=
CERTIFIED

development evidence
!=
Final-SHA certification evidence

P7 increment VERIFIED
!=
P7 milestone CERTIFIED

P7 必须到 Final Closure 后由 exact Final SHA certification ACCEPTED，
才允许进入 P8。

============================================================
7. 禁止文档“过度更新”
============================================================

本任务只做 current-truth convergence。

不要：

- 预写尚不存在的 P7.11；
- 宣称 Research Web 已实现；
- 宣称 Research Runtime 已激活；
- 宣称 P7 已完成；
- 宣称 P8 ready；
- 为未来阶段写虚假的 completed 状态；
- 用本次任务替未来 architecture design 做决定。

如果仓库当前只说明：

Research Web consumption / visualization
以及
finite Research Runtime lifecycle

仍然 open，

保持这个事实。

============================================================
8. Architecture non-regression
============================================================

本任务不得改变以下 architecture contracts：

### Research

Research 仍然不能拥有 Trading economic authority。

Research 可以拥有：

- Dataset
- Calculation
- Factor
- Target
- Sweep
- Evaluation
- Statistics
- Research Result
- Artifact
- Query

不能因为质量修复引入：

- Order
- Position
- Account
- Broker
- Settlement
- Reservation
- Durable Trading Transaction

### Query / API

保持：

ArtifactReader.load_verified()
        ↓
Research Query
        ↓
Read-only API

API 不能：

- 直接成为 semantic authority；
- 建 Query Store；
- 建 latest mutable truth；
- 绕过 Artifact verification；
- 直接读取其它上游 authority 以绕开 Artifact；
- 增加 Runtime control；
- 增加 Job submission；
- 增加交易写接口。

### Runtime

Research Runtime 如果当前仍未激活：

保持未激活。

不要在本任务实现 factory。

### Trading

不得修改正式 Trading economics。

============================================================
9. 修改范围原则
============================================================

期望修改主要集中于：

- dependency declarations；
- uv.lock；
- pytest compatibility 必要的小范围测试/配置修复；
- README；
- roadmap / validation / engineering docs 中的 stale state；
- 必要的 quality infrastructure documentation。

只有当实际 compatibility 问题要求时，
才能修改生产代码。

如果需要修改生产代码：

必须先解释 pytest/toolchain 为什么暴露了真实 bug，
并补对应测试。

============================================================
10. 质量检查策略
============================================================

遵守 OnlyAlpha 当前质量工具链设计：

单小节任务不默认无脑运行所有昂贵的 milestone certification。

但本任务的特殊性是：

它修改 pytest major-version / workspace dependency contract /
quality infrastructure baseline。

因此影响面不是单一业务函数。

必须使用：

targeted verification
→ affected verification
→ broad quality verification

逐级扩大。

============================================================
11. 第一层：快速 targeted checks
============================================================

根据实际修改范围选择。

至少包括类似：

uv sync --frozen --all-packages --all-groups

uv run pytest <直接受影响测试>

uv run ruff check <affected paths>

uv run ruff format --check <affected paths>

如果修改 onlyalpha-api：

执行 API package 的：

- unit tests；
- architecture tests；
- type checks；
- package build；
- research-query integration regression。

============================================================
12. 第二层：受影响 canonical lanes
============================================================

由于 pytest 是整个 workspace test runner，
不能只执行 API tests。

必须运行当前 scripts/test_suite.py 中与修改影响面相符的 canonical lanes。

至少应检查：

- research-query
- research-artifact

并根据当前 test suite topology 判断其它受影响 lanes。

重点：

不要凭文件名猜影响范围。

应检查：

- test imports；
- shared fixtures；
- conftest；
- package dependency；
- pytest config；
- workspace configuration；
- test_suite.py lane composition。

============================================================
13. 第三层：质量基础设施验证
============================================================

由于本任务改变 pytest/toolchain contract，应执行足以证明：

“测试工具升级没有破坏工程验证体系”

的验证。

至少包括：

- static checks
- architecture checks
- build checks
- dependency audit
- broad canonical tests

如果当前仓库质量方案定义了：

uv run python scripts/test_suite.py core-full

作为这一类“阶段/大范围验收”命令，

本任务属于适合执行该命令的情况：

因为 pytest 是全工程测试基础设施。

因此，在 targeted/affected tests 已经稳定后，
允许并建议在最终验收阶段执行：

uv run python scripts/test_suite.py core-full

如果仓库当前的正式命令或 suite 名称已经变化，
以 current HEAD 为准。

============================================================
14. Coverage
============================================================

如果 pytest upgrade 改变 coverage execution：

必须确认：

- branch coverage 仍正常；
- coverage source 没变化；
- omit/include 没被错误改变；
- package-local coverage contract 没丢失；
- 没通过降低 threshold 解决问题。

不要因为这个任务修改业务 coverage threshold，
除非能证明原 threshold 配置本身存在 bug。

============================================================
15. GitHub Actions / CI 验证
============================================================

本地验证通过后：

检查 CI workflow 是否仍然符合当前质量方案。

不得为了适配 pytest 9 修改 CI 语义，
除非当前 CI 存在真实兼容问题。

如果可访问 GitHub Actions：

检查 exact resulting HEAD：

- Layered Quality
- CodeQL
- dependency-audit
- 相关 main lanes

最终结果必须区分：

LOCAL VERIFIED

和：

REMOTE VERIFIED

如果由于任务环境无法等待或执行远端 CI：

不要宣称 remote green。

明确输出：

LOCAL VERIFIED
REMOTE CI PENDING

============================================================
16. Final-SHA Certification
============================================================

本任务不是 P7 Final Closure。

不要因为这次质量收敛擅自运行或宣称：

P7 Final-SHA Certification ACCEPTED

除非仓库当前正式阶段明确要求本次任务就是 milestone closure。

正常情况下：

本任务只恢复 clean P7 development baseline。

P7 Final Certification 留到真正 P7 Final Closure。

============================================================
17. 测试失败处理
============================================================

如果发现测试失败：

先分类。

### A. 本次修改导致

必须修复。

### B. 当前 master 已存在且与本任务无关

记录：

PRE-EXISTING FAILURE

提供：

- command；
- test；
- error；
- 为什么判断与本任务无关。

不要借机大范围修复。

### C. pytest 9 暴露真实不兼容

属于本任务范围。

修复并补验证。

### D. pytest 9 暴露测试代码依赖未定义行为

优先修复测试 contract，
不要依赖 pytest 旧版本偶然行为。

============================================================
18. 文档验收
============================================================

最终执行 repository-wide 搜索，确保没有明显 stale status。

搜索关键字应根据当前工程实际状态确定，例如：

P6
P7
P7.10
P7.11
Final-SHA
CERTIFIED
VERIFIED
pending
remote certification
Research Runtime
P8

检查：

README
docs/**
.github/**
validation reports

确保：

同一个状态没有相互矛盾的描述。

============================================================
19. 不要制造历史
============================================================

历史 validation 文档如果是不可变证据：

不要为了让旧文档看起来“当前正确”而篡改历史结论。

需要区分：

Historical evidence
vs
Current status documentation

例如：

某份 P7.6.2 validation report 当时写 exact SHA，
这是历史证据，不应因为 master 后来变化而替换 SHA。

应修改的是：

README / roadmap / current-status docs

而不是历史 certification evidence。

============================================================
20. 推荐实施顺序
============================================================

按以下顺序工作：

Phase A — Inspect

1. 获取 current HEAD。
2. 重新阅读 dependency declarations。
3. 确认当前 dependency-audit failure。
4. 确认 pytest version conflict。
5. 搜索 stale documentation。
6. 输出简短 root-cause summary。

Phase B — Dependency Closure

7. 修正 authoritative pytest contract。
8. 重新解析 uv.lock。
9. uv sync --frozen。
10. 验证 pytest 9 compatibility。
11. 修复必要的不兼容。

Phase C — Targeted Verification

12. API/package tests。
13. Research Query tests。
14. architecture/static checks。
15. build。

Phase D — Broad Verification

16. affected canonical lanes。
17. core-full 或当前等价 broad suite。
18. coverage。
19. dependency-audit。

Phase E — Documentation Closure

20. 修正 README current state。
21. 修正 roadmap/current validation state。
22. repository-wide stale-state search。
23. 确保历史 evidence 未被错误改写。

Phase F — Final Review

24. git diff。
25. 确认无功能扩张。
26. 确认无 architecture semantic change。
27. 确认 dependency/security gate 仍 fail closed。
28. 输出验收报告。

============================================================
21. 重点检查 Git Diff
============================================================

最终 diff 应当“小而完整”。

警惕以下异常：

- 大量业务代码改变；
- Calculation fingerprint 改变；
- Dataset schema 改变；
- Artifact schema 改变；
- Query semantic output 改变；
- API route 新增；
- Runtime factory 激活；
- Trading code变化；
- dependency scanner被放宽；
- coverage threshold降低；
- 大量 unrelated formatting churn。

如出现，应停下来重新评估范围。

============================================================
22. Definition of Done
============================================================

只有满足以下条件才能判定完成。

Dependency:

[ ] current dependency finding 已消除
[ ] pytest contract 不再互相冲突
[ ] uv.lock 由 uv 正式生成
[ ] uv sync --frozen 成功
[ ] dependency-audit 通过
[ ] 没有新增无依据 vulnerability ignore

Toolchain:

[ ] pytest 9 compatibility 已验证
[ ] shared fixtures / hooks / plugins 正常
[ ] test discovery 正常
[ ] Hypothesis/coverage 正常

Code Quality:

[ ] Ruff 通过
[ ] formatting check 通过
[ ] mypy 通过
[ ] import architecture checks 通过
[ ] Semgrep/semantic guardrails 通过（如果属于当前 gate）
[ ] build 通过

Tests:

[ ] direct affected tests 通过
[ ] research-query 通过
[ ] research-artifact regression 通过
[ ] 根据 dependency topology 选择的 affected lanes 通过
[ ] broad/core-full verification 通过

Documentation:

[ ] README current state 与事实一致
[ ] P6 certification state 正确
[ ] P7 仍明确为 IN_PROGRESS（如果当前事实仍如此）
[ ] P7 increment VERIFIED 与 milestone CERTIFIED 没混淆
[ ] Research Runtime 状态准确
[ ] 下一 semantic direction 准确
[ ] P8 entry gate 准确
[ ] 无明显 stale current-state description
[ ] 历史 validation/certification evidence 未被篡改

Architecture:

[ ] 没有新增业务功能
[ ] 没有新增 Authority
[ ] 没有改变 Research/Trading 边界
[ ] 没有激活 Research Runtime
[ ] 没有增加 API write/control route
[ ] 没有改变交易经济语义

============================================================
23. 最终输出格式
============================================================

完成后给出结构化报告。

# OnlyAlpha Mainline Quality & Documentation Closure

## 1. Current HEAD

- Before SHA:
- After SHA（如果已 commit）:
- Branch:

## 2. Root Causes Found

列出实际发现的问题。

例如：

- vulnerable pytest lock；
- package version constraint conflict；
- pytest compatibility issue；
- stale README status；
- stale roadmap statement。

只写实际发现，不要照抄预期。

## 3. Changes Made

逐项列出：

- file
- change
- reason
- affected contract

## 4. Dependency Closure

说明：

- before pytest version
- after pytest version
- vulnerability status
- lockfile status
- dependency-audit result

## 5. Documentation Closure

说明：

- 哪些 current-state 文档被更新；
- 哪些 historical evidence 被刻意保持不变；
- P6 / P7 / P8 状态最终是什么。

## 6. Verification

完整列出实际运行的命令和结果。

格式：

PASS
<command>

FAIL
<command>
<reason>

不要只写“tests passed”。

## 7. Architecture Review

明确回答：

- 是否改变业务语义：YES / NO
- 是否增加 Authority：YES / NO
- 是否改变 Query read-only boundary：YES / NO
- 是否激活 Research Runtime：YES / NO
- 是否改变 Trading economics：YES / NO
- 是否增加产品功能：YES / NO

本任务预期这些答案全部为 NO。

## 8. Remaining Issues

如果没有：

None.

如果有：

明确区分：

- blocking
- non-blocking
- pre-existing
- remote-CI-pending

## 9. Final Verdict

只能从以下状态中选择：

ACCEPTED — CLEAN DEVELOPMENT BASELINE

或

REJECTED — BLOCKING ISSUE REMAINS

不要把：

ACCEPTED — CLEAN DEVELOPMENT BASELINE

写成：

P7 CERTIFIED

这不是同一个含义。

============================================================
24. 最终目标
============================================================

完成本任务后的仓库应该具备这样的性质：

当前 master 是一个：

- dependency-safe；
- quality-gate-consistent；
- documentation-current；
- architecture-unchanged；
- testable；
- 可作为下一 P7 increment 起点

的 clean development baseline。

不要在本任务中开始下一阶段功能。