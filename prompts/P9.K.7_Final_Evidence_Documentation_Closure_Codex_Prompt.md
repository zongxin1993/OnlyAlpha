# OnlyAlpha — P9.K.7 Final Evidence / Documentation Closure — Codex Task Prompt

## 0. Task Identity

**Repository:** `zongxin1993/OnlyAlpha`  
**Task:** `P9.K.7 Final Evidence / Documentation Closure`  
**Task type:** narrowly scoped Task-Gate closure correction  
**Parent increment:** `P9.K.7 — Remote Protocol Foundation`  
**Observed task base:** `dfebfd4eeafe8aaa187133e6437e97d879de9f13`  
**Release version:** keep `0.9.7`  
**Next authorized increment:** `P9.K.8 — Seal Kernel`  
**P9.1+:** remains blocked until P9.K closure

This task exists for one purpose:

> **Make the committed repository evidence and current K7 documentation agree with the already-correct P9.K.7 implementation, then stop.**

This is **not** a new feature task, not a redesign, not another general audit, and not P9.K.8 work.

---

# 1. First Principle

The current K7 implementation has already passed the required committed-HEAD evidence for the previous closure fix.

At the observed base `dfebfd4eeafe8aaa187133e6437e97d879de9f13`, the required K7 closure jobs were already green:

```text
static            PASS
architecture      PASS
gateway-protocol  PASS
build             PASS
dependency-audit  PASS
```

The remaining problem is not K7 core correctness.

The remaining problem is:

```text
implementation truth
        =
CI truth

but

documentation truth
        !=
implementation / CI truth
```

Therefore this task must repair **only the stale documentation/evidence state**.

Do not use this task to reopen already-closed K7 architecture decisions.

---

# 2. Mandatory Scope Freeze

There are exactly **two known blocking findings** in scope.

## F-K7-FINAL-001 — Closure report still says NOT CLOSED

Current file:

```text
docs/reports/p9_k7_remote_protocol_foundation.md
```

still contains the pre-commit closure state:

```text
P9.K.7 Closure Fix = NOT CLOSED
P9.K.8 = DO NOT START
```

and explains that committed-HEAD CI was still required.

That statement was historically correct before `dfebfd4...` was committed and CI completed.

It is no longer the current final closure fact.

### Required fix

Preserve the historical pre-commit section.

**Do not delete or rewrite history.**

Append a new final committed-HEAD closure section that records:

```text
closure subject SHA
required committed-HEAD CI results
final closure verdict
permission to proceed to P9.K.8
```

The new section must make it unambiguous that:

```text
dfebfd4eeafe8aaa187133e6437e97d879de9f13
```

closed the previous K7 closure findings and that the five required committed-HEAD jobs passed.

If the repository has advanced before execution, record both:

```text
original closure-fix verified SHA = dfebfd4...
current documentation-closure task SHA = current HEAD after this task
```

Do not falsify the historical subject.

---

## F-K7-FINAL-002 — Current K7 design document has stale protobuf pin

Current file:

```text
docs/p9_k7_remote_gateway_protocol.md
```

still states:

```text
grpcio-tools 1.73.1
grpcio       1.73.1
protobuf     6.31.0
```

but the actual executable protocol toolchain authority is now:

```text
grpcio-tools 1.73.1
grpcio       1.73.1
protobuf     6.33.5
```

and:

```text
packages/protocol/onlyalpha-gateway-protocol/pyproject.toml
```

also pins:

```text
protobuf==6.33.5
```

### Required fix

Update only the stale current design statement so it matches the current canonical implementation:

```text
protobuf 6.31.0
→
protobuf 6.33.5
```

Add a short clarification that this pin was updated by the P9.K.7 post-commit security closure and does **not** change:

```text
canonical .proto semantics
protocol major
descriptor identity
Gateway business semantics
```

Do not redesign protocol versioning.

---

# 3. Repository Truth Before Editing

Before modifying anything:

```bash
git status --short
git rev-parse HEAD
```

Record:

```text
TASK_BASE_SHA=<actual current HEAD>
```

Then read the current versions of:

```text
project-state.toml

docs/engineering/convergent-audit-policy.md
docs/engineering/quality-system.md
docs/engineering/project-state-authority.md

docs/p9_k_stateful_kernel_protocol_boundary.md
docs/p9_k7_remote_gateway_protocol.md
docs/reports/p9_k7_remote_protocol_foundation.md

scripts/gateway_protocol.py
packages/protocol/onlyalpha-gateway-protocol/pyproject.toml

.github/workflows/quality.yml
```

Also confirm the current source still says:

```text
PROTOBUF_VERSION = "6.33.5"
```

and protocol package dependency still says:

```text
protobuf==6.33.5
```

If the repository has legitimately moved beyond the observed state, current repository truth wins.

Do **not** reset to `dfebfd4...`.

Do not begin a new broad audit.

---

# 4. Modification Scope

Expected modified files:

```text
docs/p9_k7_remote_gateway_protocol.md
docs/reports/p9_k7_remote_protocol_foundation.md
```

Optional only if deterministic projection tooling requires it:

```text
README.md
docs/roadmap.md
docs/p9_k_stateful_kernel_protocol_boundary.md
```

But these projection files should normally remain unchanged because `project-state.toml` already declares:

```text
P9.K.7 = TASK COMPLETE / VERIFIED
P9.K.8 = IMPLEMENTATION READY
```

Do not hand-edit projected current-state files.

If `project_state.py check` reports drift, use the repository's supported render mechanism rather than manually editing projections.

---

# 5. Explicitly Forbidden Changes

Do not modify:

```text
src/onlyalpha/**
packages/api/onlyalpha-api/**
packages/client/onlyalpha-client/**
packages/protocol/onlyalpha-gateway-protocol/src/**
contracts/gateway/**
contracts/research-api/**
migrations/**
```

Do not change:

```text
Strategy semantics
Research semantics
P9.0 semantics
Kernel behavior
Gateway client behavior
Gateway RPCs
Gateway message schema
command identity
stream semantics
persistence
database schema
OpenAPI
version number
```

Do not add:

```text
new abstractions
new managers
new registries
new adapters
new compatibility layers
new tests unrelated to the two findings
new protocol RPCs
QMT
CTP
Binance
P9.K.8 implementation
P9.1 implementation
```

If a code change appears necessary, stop and first prove why one of the two documentation defects cannot be fixed without changing behavior.

The expected result of this task is **documentation/evidence-only**.

---

# 6. Historical Evidence Must Remain Historical

The existing report currently records an important historical fact:

```text
the original committed K7 HEAD exposed a post-commit closure problem
```

and later:

```text
the closure-fix worktree was not yet an immutable committed CI subject
```

Do not erase those statements.

They describe different points in time.

The correct structure is:

```text
Original K7 implementation evidence
        ↓
Post-Commit Closure Correction
        ↓
pre-commit closure state: NOT CLOSED
        ↓
Committed-HEAD Final Closure Verification
        ↓
CLOSED
```

This preserves provenance and makes the evidence chain append-only.

Do not mutate history to make the report look cleaner.

---

# 7. Required Final Closure Section

Append a final section to:

```text
docs/reports/p9_k7_remote_protocol_foundation.md
```

Use a title equivalent to:

```text
## Committed-HEAD Final Closure Verification
```

It must contain at least:

```text
Original P9.K.7 Task Base SHA
Original K7 implementation SHA
Closure-fix base SHA
Closure-fix committed SHA = dfebfd4eeafe8aaa187133e6437e97d879de9f13
Current documentation-closure task base/final SHA
```

Record the final required evidence:

```text
static            PASS
architecture      PASS
gateway-protocol  PASS
build             PASS
dependency-audit  PASS
```

Also summarize the three closure corrections already proven:

```text
1. Architecture current-tree verification no longer depends on undeclared clone history.
2. Historical K7 task delta has one explicit full-history authority and fails closed if baseline is missing.
3. Stream application error taxonomy is exact and protobuf is safely pinned to 6.33.5.
```

Record semantic deltas:

```text
Product OpenAPI semantic delta = 0
protected P9 semantic delta    = 0
canonical Proto semantic delta = 0
protocol major                 = onlyalpha.gateway.v1
business persistence delta     = 0
```

Then final verdict:

```text
P9.K.7 Final Evidence / Documentation Closure = CLOSED

BLOCKER = 0
MAJOR   = 0

P9.K.7 = TASK COMPLETE / VERIFIED
P9.K.8 = MAY START / IMPLEMENTATION READY
P9.1+  = remains blocked until P9.K closure
```

Do not call P9.K itself DONE.

Do not call P9.K.7 Final-SHA Certified unless an actual Certification Gate exists for that exact claim.

This is Task-Gate closure only.

---

# 8. Required Design Document Correction

In:

```text
docs/p9_k7_remote_gateway_protocol.md
```

change the exact toolchain text from:

```text
grpcio-tools 1.73.1
grpcio       1.73.1
protobuf     6.31.0
```

to:

```text
grpcio-tools 1.73.1
grpcio       1.73.1
protobuf     6.33.5
```

Add one concise sentence similar to:

```text
The protobuf pin was updated to 6.33.5 by the P9.K.7 post-commit security closure; canonical .proto bytes, protocol major, generated descriptor identity, and Gateway semantics remained unchanged.
```

Do not add an extended security essay.

Do not duplicate the entire closure report here.

---

# 9. Project-State Authority

`project-state.toml` is the sole current engineering progression authority.

Expected state remains:

```text
last_verified_increment = "P9.K.7"
last_verified_state = "TASK COMPLETE / VERIFIED"

next_authorized_increment = "P9.K.8"
next_authorized_state = "IMPLEMENTATION READY"

p9_1_plus_status = "BLOCKED until P9.K closure"
```

Do not edit this state merely because this documentation closure runs after K7.

The task is repairing historical/current evidence consistency around an already-verified increment.

Required command:

```bash
uv run python scripts/project_state.py check
```

must pass.

---

# 10. Minimal Verification Plan

This task must use the **minimum sufficient verification**.

Do not run an open-ended repository-wide audit.

## V1 — Diff scope

```bash
git diff --name-only
```

Expected normal result:

```text
docs/p9_k7_remote_gateway_protocol.md
docs/reports/p9_k7_remote_protocol_foundation.md
```

Any code/schema/protocol file in the diff is a failure unless explicitly justified.

---

## V2 — Project state consistency

```bash
uv run python scripts/project_state.py check
```

Must PASS.

---

## V3 — Protocol authority consistency

Confirm mechanically or by exact source inspection:

```text
scripts/gateway_protocol.py
PROTOBUF_VERSION == "6.33.5"

packages/protocol/onlyalpha-gateway-protocol/pyproject.toml
protobuf==6.33.5

docs/p9_k7_remote_gateway_protocol.md
protobuf 6.33.5
```

All three must agree.

---

## V4 — Gateway deterministic projection remains unchanged

Because this task must not change the protocol:

```bash
uv run python scripts/gateway_protocol.py check
```

Must PASS.

No generated protocol file should change.

---

## V5 — Targeted K7 architecture/contract evidence

Run the smallest directly applicable existing evidence:

```bash
uv run pytest \
  tests/architecture/test_p9_k7_remote_protocol_boundary.py \
  tests/contracts/test_gateway_protocol_contract.py \
  tests/contracts/test_p9_k7_task_delta.py \
  -q
```

If the historical task-delta test requires full Git history and the local clone genuinely lacks it, do not weaken the test.

Either fetch the required immutable history using the repository-approved method or report that specific local evidence as unavailable while preserving fail-closed behavior.

Do not convert this into a reason to redesign the test again.

---

## V6 — Documentation sanity

Check:

```bash
git diff --check
```

Must PASS.

Also search the current K7 design document for stale executable pin:

```text
protobuf 6.31.0
```

It must no longer appear as the current toolchain requirement.

Historical closure-report references to the old vulnerable version are allowed and must remain clearly historical.

---

# 11. CI Scope

This documentation-only closure must not restart a repository-wide certification campaign.

If normal CI runs automatically for the final commit, observe its normal result.

Do not invent new mandatory jobs.

If one of the following directly relevant K7 jobs fails because of this task:

```text
static
architecture
gateway-protocol
build
dependency-audit
```

investigate and fix the regression.

If an unrelated existing Phase/Certification lane fails for a pre-existing reason:

```text
report it separately
do not expand this task
```

The purpose of this task is not to clear every repository warning.

---

# 12. Audit Discipline — No Endless Audit

This section is mandatory.

The Codex execution must not perform repeated generic audits after the two known findings are fixed.

Use this exact rule:

```text
Start findings:
F-K7-FINAL-001
F-K7-FINAL-002
```

For each, final status must be one of:

```text
RESOLVED
NOT_RESOLVED
REGRESSED
```

New BLOCKER/MAJOR findings are allowed only if:

```text
new concrete evidence
+
violation of an already-applicable frozen rule
+
actual impact
+
introduced by this documentation closure
```

Do not create a new finding for:

```text
naming preference
future hardening
possible future TLS design
possible future provider behavior
general code style
optional tests
P9.K.8 work
P9.1 work
pre-existing migration surfaces explicitly assigned to K8
```

Do not re-audit all of P9.K.0–K7.

Do not re-open already-green K7 implementation decisions.

---

# 13. Mandatory Stop Condition

As soon as all of the following are true:

```text
F-K7-FINAL-001 = RESOLVED
F-K7-FINAL-002 = RESOLVED

BLOCKER = 0
MAJOR   = 0

project_state.py check = PASS
gateway_protocol.py check = PASS
targeted K7 tests = PASS or explicitly CI-backed where local immutable history is unavailable
git diff --check = PASS

no code/protocol/schema semantic delta
```

**STOP THE TASK.**

Do not perform another speculative audit loop.

Do not ask:

```text
"What else could be improved?"
"What future issue might exist?"
"Should we refactor this while here?"
```

Do not continue until MINOR/SUGGESTION are zero.

The purpose of the convergent audit policy is to converge.

---

# 14. Definition of Done

This task is complete only when:

```text
[1] docs/p9_k7_remote_gateway_protocol.md uses protobuf 6.33.5.

[2] docs/reports/p9_k7_remote_protocol_foundation.md preserves historical NOT CLOSED evidence.

[3] The same report appends a committed-HEAD final closure section.

[4] The final section records dfebfd4... and the five required successful CI jobs.

[5] The report clearly states P9.K.7 closure is CLOSED.

[6] The report clearly states P9.K.8 may start.

[7] It does NOT claim P9.K itself is complete.

[8] It does NOT unblock P9.1+ before P9.K closure.

[9] project-state authority is unchanged and consistent.

[10] No product/API/Kernel/Strategy/Research/Gateway protocol behavior changes.

[11] No generated protocol delta.

[12] No database/persistence delta.

[13] BLOCKER = 0.

[14] MAJOR = 0.

[15] Execution stops after these conditions are proven.
```

---

# 15. Final Response Format

Return a short engineering closure report, not another long design proposal.

Use:

```text
P9.K.7 Final Evidence / Documentation Closure

Task Base SHA:
Final SHA / worktree:

Previous findings:
F-K7-FINAL-001 — RESOLVED / ...
F-K7-FINAL-002 — RESOLVED / ...

Changed files:
...

Verification:
project-state             PASS
gateway projection        PASS
targeted K7 tests         PASS
git diff --check          PASS
protocol semantic delta   0
Product/P9 semantic delta 0

BLOCKER: 0
MAJOR:   0

Verdict:
GO

P9.K.8:
MAY START

P9.1+:
STILL BLOCKED until P9.K closure
```

Do not add a new list of speculative improvements after `GO`.

---

# 16. Final Principle

The goal is not to make P9.K.7 theoretically perfect.

The goal is to make the already-correct implementation, committed CI evidence, design documentation, and historical closure report agree with each other.

Use the smallest mechanism that closes the two known defects.

Then stop and move the project forward to P9.K.8.
